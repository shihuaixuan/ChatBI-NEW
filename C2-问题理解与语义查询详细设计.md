# C2-问题理解与语义查询详细设计

## 1. 范围与边界

本文定义问题上下文、结果引用解析、语义绑定、时间和值解析、查询编译及数据快照协议。执行架构遵循 C1：主 Agent 创建业务计划，查询工具在调用时完成正式语义绑定。本文不生成执行 DAG，不选择执行模式，不维护第二套运行状态机。

问题理解输出用户需求和语言结构；语义层提供发布态事实；查询工具将二者绑定为不可变 SemanticQueryRequest。模型不能提供 SQL、物理字段、连接路径、权限条件或计算实现。

## 2. 服务与契约

| 服务 | 输入 | 输出 | 调用时机 |
| --- | --- | --- | --- |
| QuestionContextService.prepare | 用户消息、历史理解投影、RunContext | QuestionContext | Run PREPARING |
| QuestionContextService.revise | 原修订、用户交互命令 | 新 QuestionContext | 用户明确修改或澄清 |
| SemanticContextService.retrieve | 问题、固定合约版本、权限引用 | SemanticContext | 准备及工具补充检索 |
| SemanticBindingService.bind | QueryIntent、QuestionContext、SemanticContext | BindingResult | 每次查询工具调用 |
| SemanticQueryService.compile | SemanticQueryRequest、权限引用 | CompiledQuery | 绑定通过后 |
| DataSnapshotService | 数据源、固定策略、快照引用 | 快照租约及连接上下文 | 查询派发前 |
| QueryExecutionService.execute | CompiledQuery 引用、快照引用 | ResultSet | 成本与权限校验后 |

公开接口使用强类型 DTO，禁止以任意字典替代稳定输入。服务端补齐 run_id、agent_id、step_id、权限和版本，不接受模型覆盖。

## 3. 问题上下文

### 3.1 RunContext

字段：run_id、dataset_id、datasource_id、semantic_view_id、contract_version_id、permission_snapshot_id、runtime_snapshot_id、data_snapshot_id、reference_at、timezone、question_context_revision。除准备阶段外，所有字段必填。引用必须属于当前工作空间和 Run。

### 3.2 QuestionContext

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| question_context_id | string | 稳定对象 ID |
| revision | int | 从 1 递增 |
| original_question | string | 当前用户原文 |
| standalone_question | string | 补齐历史指代后的问题 |
| category | enum | DATA_QUERY / SEMANTIC_DEFINITION / NON_ANALYTICAL |
| requirements | RequirementClause[] | 用户明确要求的结果 |
| immutable_constraints | UserConstraint[] | 数据范围、时间、筛选及明确排除项 |
| result_references | ResolvedResultReference[] | 历史实体引用，仅用于理解 |
| confirmed_bindings | ConfirmedBinding[] | 当前版本内用户已确认的候选 |
| source_message_ids | string[] | 信息来源 |
| reference_at / timezone | datetime / string | 时间解释基准 |
| fingerprint | string | 规范化内容指纹 |

`RequirementClause = {requirement_id, source_message_id, source_start, source_end, description, requested_output}`。requested_output 取 VALUE、DETAIL、COMPARISON、TREND、RANKING、ANOMALY、ATTRIBUTION、DRILLDOWN、CHART、REPORT、DEFINITION。原文范围使用 Unicode 码点索引，左闭右开。

`UserConstraint = {constraint_id, source_message_id, phrase, kind, resolution_status, binding_ref?}`。kind 为 DATASET、METRIC、DIMENSION、FILTER、TIME、EXCLUSION。模型可抽取语言候选，程序校验来源、字段和冲突；未确认歧义不能变成可执行绑定。

新修订只接受当前用户的正式交互命令，不能由 Agent 的 PlanPatch 修改。需求删除、时间变更和筛选调整保留来源与历史修订。既有 SemanticQueryRequest 不随修订改变。

### 3.3 入口流程

```text
输入长度与空值校验
→ 读取允许的历史理解上下文
→ question_rewrite
→ intent_recognition 与 slot_extraction
→ temporal_interpretation
→ 校验原文来源、需求与约束
→ 保存 QuestionContext
→ 检索发布态语义上下文
→ 返回 Agent 规划上下文
```

准备流程不冻结具体工具计划。DATA_QUERY 进入主 Agent 规划；SEMANTIC_DEFINITION 通过语义事实工具生成可引用结果；NON_ANALYTICAL 按 C1 以 FAILED + NON_ANALYTICAL_REQUEST 结束，返回固定能力说明，不创建业务计划或触发查询。

## 4. 连续追问与结果引用

### 4.1 继承规则

| 内容 | 新 Run 处理 |
| --- | --- |
| 数据集 | 由会话绑定固定，模型不能更改 |
| 指标、维度和筛选意图 | 继承用户已确认内容，在新发布版本重新绑定 |
| 相对时间 | 按新 Run.reference_at 重新解析 |
| 明确绝对时间 | 保留原始时间要求 |
| 历史 SQL、权限条件和执行计划 | 不进入新查询输入 |
| 历史数字与 Evidence | 只用于理解，不支撑新结论 |
| “这些客户”等结果指代 | 从有权读取的历史结果解析实体键，再查询新数据 |

历史实体集合上限为 1000。超过上限返回 RESULT_REFERENCE_LIMIT_EXCEEDED；不能只取前 1000 个。指代“前 N 个”必须存在已保存的稳定顺序；实体键不唯一时返回 RESULT_REFERENCE_AMBIGUOUS。用户看到的标签不能替代唯一实体键。

### 4.2 ResolvedResultReference

字段：source_run_id、source_result_id、source_fingerprint、entity_key_ref、entity_values、selection_rule、current_permission_checked_at。selection_rule 取 ALL_SAVED、EXPLICIT_TOP_N、EXPLICIT_SELECTION。历史结果必须完整覆盖用户所指集合；系统截断结果不能解释“全部这些对象”。

解析出的集合成为新 QueryIntent 的筛选条件，并记录来源。新 Run 仍使用新的语义、权限与数据快照。历史字段在新版本中不存在时返回 SEMANTIC_REFERENCE_UNAVAILABLE，禁止按物理字段名替代。

## 5. 模型与确定性处理边界

模型任务由 C5 注册。各阶段结构如下：

| 输出 | 必需字段 | 禁止内容 |
| --- | --- | --- |
| QuestionRewriteOutput | standalone_question、context_operations、retrieval_phrases | SQL、资产 ID、旧计划 |
| IntentOutput | category、requested_outputs、requirement_candidates | 执行器、工具选择、运行状态 |
| SlotOutput | metric_phrases、dimension_phrases、filter_phrases、detail_phrases、order_phrases | 物理字段、权限谓词 |
| TemporalOutput | phrases、reference_kind、period_type、offset、granularity、comparison_kind | 未经验证的数据库时间表达式 |

所有候选带原文或已确认消息来源。资产选择只能引用当前 SemanticContext 中的候选 key；是否有效、是否唯一、是否允许组合由绑定服务决定。模型置信度不作为消除业务歧义的授权依据。

## 6. 语义检索与绑定

### 6.1 SemanticContext

字段：contract_version_id、semantic_view_id、context_id、context_fingerprint、assets、capabilities、analysis_paths、candidate_groups、limitations、omitted_asset_count。内部传输为 JSON；调用模型时按 A 的结构生成 YAML。无物理表、字段、JOIN、权限表达式和连接凭证。

检索先限定发布版本与权限，再召回资产及必要依赖，最后按问题相关性控制上下文范围。补充检索只能扩大当前语义视图内的上下文，不能改变 Run 的版本和权限。

### 6.2 QueryIntent

模型可提交的查询意图：

```json
{
  "metric_keys": ["gmv"],
  "dimension_keys": ["channel"],
  "detail_field_keys": [],
  "time": {"phrase":"本月","comparison":"PREVIOUS_PERIOD"},
  "filters": [],
  "order_by": [{"key":"gmv","direction":"DESC"}],
  "limit": 20,
  "purpose": "PERIOD_COMPARISON",
  "requirement_ids": ["req_1"],
  "candidate_context_id": "ctx_1"
}
```

尚未获得唯一 key 时允许提交对应 phrase，key 与 phrase 为互斥联合，不允许同时填写。服务端核验 key 属于给定上下文和固定发布版本。时间、筛选、排序与 limit 必须来自用户表达、确认记录或发布默认值。

### 6.3 绑定顺序

```text
校验 QuestionContext 修订
→ 绑定指标、实体和明细字段
→ 绑定维度与值
→ 校验指标维度能力
→ 解析时间、粒度和对比关系
→ 合并用户不可变筛选、指标固定口径
→ 校验排序、Top N 和输出范围
→ 生成 SemanticQueryRequest
```

BindingResult.status 为 VALID、AMBIGUOUS、UNSUPPORTED。AMBIGUOUS 返回带候选的 AmbiguityItem，由 C1 ConfirmationManager 保存；当前工具转 WAITING_CONFIRMATION，不直接访问前端。UNSUPPORTED 形成不可技术重试的工具错误。

必须澄清：多个业务口径、同名但不同业务域、多个合法时间角色、值映射多义、用户要求之间冲突。未发布资产、缺少安全关系、无可用分析能力返回不支持，不通过澄清授权绕过。

## 7. 时间、值与过滤协议

### 7.1 时间

TimeBinding 字段：calendar_key、time_dimension_key、timezone、start、end_exclusive、grain、comparison_kind、alignment_policy、source_phrase。时间区间统一左闭右开。reference_at 固定，恢复不重新读取当前时间。

自然月、周、财年和营业日按发布的 semantic_calendar 计算。同比按日历映射对齐；闰日、月末和不完整当期使用发布的 alignment_policy。未声明策略时返回 TIME_ALIGNMENT_UNSUPPORTED，不默认按 365 天平移。

### 7.2 值

ValueBinding 字段：dimension_key、standard_value_keys、storage_value_refs、mapping_version、match_kind、source_phrase。match_kind 为 EXACT、ALIAS、USER_CONFIRMED。原始存储值只在编译服务可见，模型使用标准值。

字符串比较的大小写、空白和字符归一化由发布映射决定。找不到值返回 VALUE_UNRESOLVED；多义返回 VALUE_AMBIGUOUS。NULL 使用 IS_NULL / IS_NOT_NULL，不以字符串 `"null"` 表达。

### 7.3 FilterExpr

递归判别联合：

- `{op: AND | OR, items: FilterExpr[]}`，items 非空。
- `{op: NOT, item: FilterExpr}`。
- `{op: EQ | NE | GT | GE | LT | LE | IN | NOT_IN | BETWEEN | IS_NULL | IS_NOT_NULL, dimension_key, values: TypedValue[]}`。

操作符必须在维度发布能力中。BETWEEN 包含两个有序值；IS_NULL / IS_NOT_NULL 的 values 为空；IN / NOT_IN 非空且不超过 1000 项。WHERE 与聚合后过滤分开，指标条件进入 having。权限过滤不属于 QueryIntent，由编译阶段单独注入且不能被用户 OR 条件包裹。

## 8. SemanticQueryRequest 与编译

字段：query_id、run_id、agent_id、step_id、question_context_revision、requirement_ids、contract_version_id、semantic_view_id、permission_snapshot_id、data_snapshot_id、select_mode、metric_keys、dimension_keys、detail_field_keys、time_bindings、filters、having、order_by、limit、purpose、analysis_contract_ref?、fingerprint。

select_mode 为 METRIC / DETAIL。purpose 为 VALUE、DETAIL、PERIOD_COMPARISON、TREND、RANKING、CONTRIBUTION、FORMULA、DRIVER、DRILLDOWN。编译请求不可修改；参数变化创建新的 query_id。

编译算法由 A 的“确定性查询编译规范”定义，执行顺序固定：发布资产加载、能力校验、指标展开、粒度对齐、关系安全检查、权限注入、逻辑计划、方言生成、只读 AST 校验、成本估算。

CompiledQuery 字段：compiled_query_id、query_id、dialect、logical_plan_ref、compiled_sql_artifact_ref、parameter_artifact_ref、result_schema、lineage、permission_fingerprint、cost_estimate、fingerprint。SQL 和绑定参数在受控制品保存，模型只能获得安全查询说明。普通用户通过 C6 投影查看 SQL。

同比环比在一个 ToolCall 内创建当前期和对比期两个 QueryExecution，并返回两份 ResultSet 引用；两者必须共享时间对齐规则和 DataSnapshotContext。工具内多查询是确定性实现细节，不创建新的业务 Plan。

## 9. 数据快照实现协议

### 9.1 生命周期

`DataSnapshotContext = {data_snapshot_id, run_id, datasource_id, mode, status, owner_id?, generation, source_token_ref?, opened_at?, expires_at?, consistency_level, capability_version}`。

mode 为 EXPORTED_TRANSACTION、PINNED_CONNECTION、SOURCE_TOKEN。status 为 CREATED、ACTIVE、EXPIRED、RELEASED、INVALID。正式分析只使用 REPEATABLE 或 STRONG 一致性；未验证能力不得声明一致快照。

首次查询前打开快照并转 ACTIVE。每次派发、重试和结果提交校验相同 data_snapshot_id、generation 和有效期。Run 终态后释放。默认物理快照最长存活 1800 秒，属于技术资源配置并写入 RuntimeSnapshot；不能自动续建新快照。

### 9.2 数据源适配要求

| 能力 | 实现 | 并行与恢复 |
| --- | --- | --- |
| 可导出事务快照 | 快照持有连接打开只读可重复读事务并导出 Token；查询连接在首条查询前导入 | 查询可跨连接并行；持有连接丢失后 Token 不可继续使用 |
| 连接内一致快照 | 固定只读事务与专属连接，由快照执行进程串行执行当前 Run 查询 | 不跨连接假设一致性；连接丢失即 INVALID |
| 数据源原生版本 Token | 适配器为每次查询附加相同版本或时间旅行 Token | 必须验证 Token 保留期、所有涉及表及权限支持 |

PostgreSQL 适配器实现 EXPORTED_TRANSACTION；MySQL/InnoDB 适配器实现 PINNED_CONNECTION。其他数据源必须实现上述一种协议并通过一致性验收后启用；仅存在数据库连接能力不代表支持智能问数一致性分析。

持有连接由专门快照执行进程维护，不占用 API 进程，不在 PostgreSQL 元数据库事务中等待业务查询。查询参数、取消命令和结果通过正式 QueryExecution 记录交接。PINNED_CONNECTION 的查询按创建顺序串行；工具和模型任务仍可并行。

### 9.3 失效与用户等待

快照失效时停止新查询，取消依赖该快照的活动查询，Run 进入 FAILED，error_code=DATA_SNAPSHOT_EXPIRED。已提交结果保留但不与新快照组合。用户重跑创建关联新 Run，重新执行分析；原 Run 的业务结果和计划不覆盖。

等待确认不创建新快照。快照未打开时可长期等待；已打开时仍受 expires_at 限制。数据源变更测试必须证明同一快照内重复查询结果不受其他事务提交影响。

## 10. 成本、完整性与缓存

查询执行前计算 estimated_rows、estimated_scan_bytes、估算可信度和策略等级。无可靠估算时标记 UNKNOWN，采用配置的保守限制，不能以零成本放行。软限制由 C1 确认；硬限制返回 RESOURCE_LIMIT_EXCEEDED。

明细默认且最大 200 行；聚合结果默认 200 行、硬上限 1000 行。工具可在数据库内完成全量聚合并输出小结果。用户 Top N 和系统截断分别标记 LIMITED_BY_USER、TRUNCATED_BY_SYSTEM；不能因为返回行数小于上限就推断总体完整。

结果复用仅限同一 Run、相同查询指纹、权限、数据快照、语义版本和 QuestionContext 约束。跨 Run 不复用数据结果。编译缓存不包含执行结果，键包含合约、权限、方言和规范化查询；权限规则变化不得复用旧编译产物。

## 11. 错误与验收

错误使用 C6 PublicError。语义错误包括 ASSET_NOT_PUBLISHED、SEMANTIC_AMBIGUOUS、VALUE_UNRESOLVED、TIME_ALIGNMENT_UNSUPPORTED、AGGREGATION_UNSAFE、QUERY_CAPABILITY_UNSUPPORTED；数据错误包括 DATA_SNAPSHOT_UNSUPPORTED、DATA_SNAPSHOT_EXPIRED、QUERY_TIMEOUT、RESULT_REFERENCE_LIMIT_EXCEEDED。

验收数据必须覆盖：同名指标；标准值别名及多义；跨月和闰日；不可变筛选；多指标不同粒度；一对多关系；去重计数；空结果；Top N 与系统截断；历史实体追问；版本变化；权限缩小；两连接快照一致性；快照持有进程失联。每个查询用例保存输入、发布合约、绑定结果、SQL、参数和预期数据，不以模型回答文本作为查询正确性判据。

## 12. 需求覆盖协议

RequirementClause 的 requested_output 在正式绑定后生成不可变 RequirementAcceptance：requirement_id、question_context_revision、expected_output_types、metric_keys、dimension_keys、time_binding_refs、constraint_fingerprint、requires_complete_population、required_chart_type?、requires_report。

该对象只定义结果验收，不定义工具顺序。生成规则固定：VALUE / DETAIL 对应查询事实；COMPARISON / TREND / RANKING 对应同名分析结果；ANOMALY / ATTRIBUTION / DRILLDOWN 对应分析结果及限制；CHART / REPORT 对应已验证制品；DEFINITION 对应 SEMANTIC_FACT。

COMPLETE_STEP 提交的 covered_requirement_ids 必须逐项匹配 RequirementAcceptance 的结果类型、资产、时间、筛选及完整性。Agent 自报覆盖不直接成立。一个需求需要多个结果时全部满足后才算覆盖；空结果可以满足“查询是否存在”的需求，但不能证明非空总体的排名或归因。

QuestionContext 修订后重新生成相应验收对象，已完成步骤不得直接沿用旧 covered_requirement_ids 宣称覆盖新需求。FinishManager 按最新修订校验全部需求，缺项进入 unmet_requirement_ids。
