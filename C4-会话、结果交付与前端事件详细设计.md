# C4-会话、结果交付与前端事件详细设计

## 1. 范围

本文定义会话、Run 视图、REST、SSE、确认、回答、图表、报告、反馈和软删除。运行状态、计划与子任务遵循 C1；结果与 Evidence 遵循 C3；权限投影和错误遵循 C6。前端不推断或修改权威业务状态。

## 2. 会话与消息

Conversation 字段：conversation_id、dataset_id、semantic_view_id、title、owner_user_id、created_at、updated_at、lock_version。数据集由用户选择并绑定会话。切换数据集创建新会话，不修改已有 Run。

ConversationMessage 字段：message_id、conversation_id、role、content、parent_run_id?、run_id?、command_id、created_at。role 为 USER / ASSISTANT / SYSTEM。用户文本原样保存；模型输出经安全校验后交付。

提交问题使用 `{command_id, content, parent_run_id?, request_kind}`。request_kind 为 NEW_QUESTION / REPORT。创建事务保存消息和 CREATED Run，返回 202 与 run_id。重复 command_id 和相同请求体返回原结果；同键不同请求体返回 IDEMPOTENCY_CONFLICT。

明确修改当前任务通过 Run 交互接口提交，不创建新问题。历史 Run 只用于理解，新数据结论必须在新 Run 查询。

## 3. RunViewSnapshot

```json
{
  "schema_version":"1.0",
  "run":{"run_id":"run_1","status":"RUNNING","completion_level":null,"scheduling_reason":null,"error":null},
  "main_agent":{"agent_id":"ag_1","role":"MAIN","phase":"EXECUTING"},
  "semantic":{"contract_version_id":"scv_12","version_changed_from_parent":false},
  "main_plan":{"plan_id":"pl_1","version":2,"goal":"分析销售额变化","steps":[]},
  "subtasks":[],
  "active_confirmation":null,
  "pending_confirmation_count":0,
  "available_results":[],
  "evidence_ids":[],
  "chart_ids":[],
  "report_id":null,
  "answer_id":null,
  "last_event_seq":42,
  "capabilities":{"can_cancel":true,"can_interact":true,"can_rerun":false,"can_view_sql":true}
}
```

main_plan.steps 包含 step_id、description、acceptance_criteria、depends_on、status、step_result_id?。subtasks 每项包含 subtask_id、parent_step_id、agent_id、role、task_name、status、phase、local_plan、result_id?、error?。LocalPlan 结构与 MainPlan 相同，禁止前端将其扁平合并为主计划。

Snapshot 在同一 PostgreSQL 一致性读取事务内取得状态与 last_event_seq。先执行当前权限检查，再投影对象；制品内容按引用另行读取。前端完整替换本地权威视图后，从 last_event_seq 继续订阅。

CREATED 显示等待执行或等待父结果；WAITING_CONFIRMATION 显示确认；FINISHING 显示校验与交付；FINISHED 按 completion_level 显示完整或部分完成。错误码提供具体失败原因，不增设另一套 Run 状态。

## 4. SSE 协议

### 4.1 连接与信封

`GET /api/chatbi/runs/{run_id}/events?after_seq={n}`，重连支持 `Last-Event-ID: {run_id}:{seq}`。同时提供时必须一致，否则返回 EVENT_CURSOR_CONFLICT。

```text
id: run_1:43
event: step.status_changed
data: {"schema_version":"1.0","run_id":"run_1","event_seq":43,"event_type":"STEP_STATUS_CHANGED","occurred_at":"2026-09-10T10:00:00Z","payload":{"agent_id":"ag_1","plan_id":"pl_1","step_id":"s1","status":"COMPLETED","step_result_id":"sr_1"}}

```

事件先持久化再唤醒。event_seq 按 Run 单调递增，与状态变更同事务提交。Redis 不保存唯一事件历史。心跳使用 SSE 注释帧，不分配序号，不写数据库。

### 4.2 事件目录

下表定义唯一领域事件与传输名称。payload 中标注 `?` 的字段允许为空，其他必填；对象完整字段遵循权威 DTO。

| 领域事件 | SSE event | payload |
| --- | --- | --- |
| RUN_CREATED | run.created | run 摘要 |
| RUN_STATUS_CHANGED | run.status_changed | old_status、status、reason_code?、completion_level? |
| AGENT_PHASE_CHANGED | agent.phase_changed | agent_id、phase |
| PLAN_CREATED | plan.created | agent_id、plan 完整结构 |
| PLAN_PATCH_APPLIED | plan.patched | agent_id、plan_id、base_version、version、plan 完整新结构 |
| STEP_STATUS_CHANGED | step.status_changed | agent_id、plan_id、step_id、status、step_result_id? |
| STEP_RESULT_CREATED | step.result_available | agent_id、step_id、step_result_id |
| SUBAGENT_CREATED | subagent.created | subtask 与 agent 摘要 |
| SUBAGENT_STATUS_CHANGED | subagent.status_changed | subtask_id、status、result_id?、error? |
| AGENT_COMMENTARY | agent.commentary | agent_id、step_id?、text |
| TOOL_PROGRESS_CHANGED | tool.progress_changed | agent_id、step_id、tool_call_id、phase、progress? |
| OBSERVATION_AVAILABLE | observation.available | agent_id、step_id、observation_id、source_type |
| CONFIRMATION_REQUIRED | confirmation.required | confirmation 完整用户视图 |
| CONFIRMATION_RESOLVED | confirmation.resolved | confirmation_id、action、next_active_confirmation_id? |
| RESULT_AVAILABLE | result.available | result_id、step_id、title、completeness |
| EVIDENCE_AVAILABLE | evidence.available | evidence_id、step_id、type |
| CHART_AVAILABLE | chart.available | chart_id、result_id |
| REPORT_AVAILABLE | report.available | report_id |
| ANSWER_AVAILABLE | answer.available | answer_id、block_ids |
| ANSWER_BLOCK_AVAILABLE | answer.block_available | answer_id、block_id、ordinal |
| RUN_FINISHED | run.finished | completion_level、answer_id、result_ids、chart_ids、report_id? |
| RESOURCE_DELETED | resource.deleted | resource_type、resource_id |

主计划和 LocalPlan 统一使用 PLAN_CREATED / PLAN_PATCH_APPLIED，以 agent_id 区分。FAILED / CANCELLED 使用 RUN_STATUS_CHANGED 携带终态，不发送 RUN_FINISHED。普通用户不接收内部执行参数、模型原始输出或权限规则。

### 4.3 顺序、补发与归并

1. 每次连接先校验当前用户与 Run 权限，读取 after_seq 后的持久化事件。
2. 历史追平后等待 Redis 通知；通知超时轮询 PostgreSQL，避免丢失提交后唤醒。
3. 客户端对 `seq <= last_seq` 去重。收到跳号先补发；确有不可见领域事件时，服务端发送同序号 `event.advance`，payload 为空，保证游标连续且不泄露内容。
4. PLAN_PATCH_APPLIED 的 base_version 必须等于本地版本；不匹配时重新读取 Snapshot，不能猜测应用顺序。
5. 事件游标过期返回 410 EVENT_CURSOR_EXPIRED；客户端读取 Snapshot 后重新订阅。
6. 权限撤销时终止连接并清除可见结果；重连重新鉴权。

`event.advance` 是可见性投影，不新增领域事件。服务端不得先过滤事件再以不连续序号要求客户端无限重连。事件载荷超过 64 KiB 时改为 `{object_ref, requires_snapshot:true}`，原序号不变；客户端按引用读取或刷新 Snapshot。

## 5. 前端状态归并

前端状态按 run_id 保存 Run、agent_id 索引、plan_id 索引、step_id 状态、subtask_id、确认、结果目录和 last_seq。step_id 的全局引用始终携带 plan_id，避免不同 LocalPlan 重名。

- Snapshot 替换权威状态，UI 展开折叠等本地设置保留。
- 计划事件替换对应 Agent 的计划结构，不覆盖其他 Agent。
- 结果事件只登记引用，内容通过读取接口获取。
- commentary 与工具进度作为附属信息，不能把步骤标记为成功。
- 所有终态按钮由 capabilities 决定，不根据文字或事件名称推断。

## 6. 确认、修改与取消

ConfirmationView 字段：confirmation_id、agent_id、step_id、type、question、options、resume_strategy、status、created_at。options 每项包含 option_id、label、description?；最多 8 项，用户只能提交服务端提供的 option_id 或允许的补充文本。

`POST /runs/{run_id}/interactions` 输入：command_id、interaction_type、confirmation_id?、selected_option_id?、content?、expected_context_revision。interaction_type 为 CONFIRM、REJECT、CLARIFY、MODIFY_REQUEST。

确认使用 C1 的队列和恢复策略。重复响应幂等；已处理确认使用不同响应再次提交返回 CONFIRMATION_ALREADY_RESOLVED。等待时间不由前端改变 Run 状态；快照失效由服务端返回明确失败及重跑能力。

`POST /runs/{run_id}/cancel` 输入 command_id。接口返回已接收的 CANCELLING 状态，不等待数据源结束。前端停止重复提交但继续订阅，直到 CANCELLED 或先于取消提交的其他终态。取消后的正式结果仍可在权限允许时读取。

`POST /runs/{run_id}/rerun` 创建关联新 Run，不恢复终态 Run。进程故障恢复属于服务端内部恢复，不向用户暴露任意恢复阶段参数。

## 7. ResultSet 与 Evidence 交付

ResultSetView 字段：result_id、run_id、agent_id、step_id、title、kind、schema、rows、row_count、completeness、limitations、query_detail?、created_at。一次返回已保存的完整有界行集合，不提供分页参数。读取前校验当前权限、软删除与制品指纹。

query_detail 包含安全 SQL、脱敏参数、指标/维度名称、时间与用户筛选。权限规则值、规则来源和注入 Trace 不进入普通视图，遵循 C6。

EvidenceView 返回类型、事实、来源引用和限制，不返回物理路径。来源跳转仍需独立鉴权。ResultSet 和 Evidence 不支持客户端更新；排序只改变页面视图，不回写结果。

## 8. 回答与数字绑定

### 8.1 输入与校验

```json
{
  "title":"销售额变化",
  "content_template":"本月销售额为 {{sales}}，较上月变化 {{change}}。",
  "numeric_bindings":[
    {"name":"sales","evidence_id":"ev_1","source_path":"/facts/current/value","format_key":"currency"},
    {"name":"change","evidence_id":"ev_1","source_path":"/facts/change_rate/value","format_key":"percent"}
  ],
  "evidence_ids":["ev_1"]
}
```

数字占位符与 binding 必须一一对应，name 在块内唯一。JSON Pointer 路径必须指向合法事实值；format_key 来自指标发布格式。模型不能增加任意计算表达式。事实数字、日期与排名由绑定生成；章节序号等非事实数字由渲染器生成。

校验顺序：Schema、引用归属、当前权限、证据有效性、数值类型、需求覆盖、Markdown 安全。失败返回给 FinishManager 修正，不输出未经验证的块。

### 8.2 Answer 与流式交付

Answer 字段：answer_id、run_id、version、status、completion_level、block_ids、evidence_ids、limitations、created_at。status 为 VALIDATED / PUBLISHED，正文不可变。

首期整份回答校验通过后在同一提交事务保存 Answer、块元数据、FINISHED 状态、全部可重放事件和审计。SSE 按 ordinal 发送 ANSWER_BLOCK_AVAILABLE，实现按块交付；不直接转发模型 Token。断线重连不会重复创建回答。

## 9. 图表

ChartSpec 字段：chart_id、run_id、result_id、chart_type、title、field_bindings、series、format、limitations、fingerprint。chart_type 为 TABLE、LINE、BAR、STACKED_BAR、PIE、SCATTER、KPI。

| 条件 | 默认图表 | 限制 |
| --- | --- | --- |
| 单个标量 | KPI | 多个指标分别展示 |
| 时间与数值 | LINE | 时间桶可排序 |
| 分类与数值 | BAR | 稳定排序 |
| 非负构成且有总体 | PIE | 最多 12 类，其他必须是可对账 OTHER |
| 两个数值变量 | SCATTER | 每行具有同一实体或分组含义 |
| 明细或不满足图形条件 | TABLE | 保留完整性标记 |

用户切换图表创建新 ChartSpec，复用同一 ResultSet。图表字段只能引用结果 Schema，不执行新 SQL。图形不适用时返回 CHART_TYPE_UNSUPPORTED，不静默改为其他类型。

## 10. 报告

报告仅在用户明确要求时生成。ReportDocument 字段：report_id、run_id、title、version、blocks、source_result_ids、source_evidence_ids、limitations、created_at。Block 为 HEADING、TEXT、TABLE、CHART、LIMITATION；TEXT 使用回答同一数字绑定协议。

MAIN 委派 REPORT_AGENT 后，子 Agent 创建 LocalPlan，读取授权结果并调用 build_report。图表和数字引用在保存前全部校验。首期交付为系统内页面，不提供邮件、外链公开发布或定时外部分发。

用户要求对历史分析生成报告时创建 REPORT Run；历史内容只用于重建分析需求，MAIN 完成新查询后再委派报告任务。不得直接把历史数据作为新 Run 的证据。

## 11. 反馈、软删除与通知

反馈类型为 CORRECT、INCORRECT、INCOMPLETE、OTHER；保存 feedback_id、run_id、target_ref?、user_id、type、comment、created_at，不修改原结果。

软删除记录 user_id、resource_type、resource_id、deleted_at。删除会话隐藏其消息与 Run；活动 Run 同事务提交取消请求。业务事实、审计和事件按保留策略存储，不物理级联删除。

MONITOR Run 使用同一 Agent 架构、独立快照和较低优先级。Notification 包含 notification_id、owner_user_id、run_id、type、title、read_at?。只发送系统内结果可用或失败通知；读取通知不能绕过来源 Run 权限。

## 12. REST 接口

| 方法与路径 | 功能 | 成功状态 |
| --- | --- | --- |
| POST /api/chatbi/conversations | 创建会话 | 201 |
| GET /api/chatbi/conversations | 会话列表 | 200 |
| GET /api/chatbi/conversations/{id} | 会话与消息 | 200 |
| POST /api/chatbi/conversations/{id}/questions | 创建问题和 Run | 202 |
| GET /api/chatbi/runs/{id}/snapshot | 权威视图 | 200 |
| GET /api/chatbi/runs/{id}/events | SSE | 200 |
| POST /api/chatbi/runs/{id}/interactions | 确认、澄清和修改 | 202 |
| POST /api/chatbi/runs/{id}/cancel | 取消 | 202 |
| POST /api/chatbi/runs/{id}/rerun | 创建关联新 Run | 202 |
| GET /api/chatbi/results/{id} | 结果及安全查询详情 | 200 |
| GET /api/chatbi/evidence/{id} | 证据 | 200 |
| GET /api/chatbi/answers/{id} | 回答与块 | 200 |
| GET /api/chatbi/charts/{id} | 图表 | 200 |
| POST /api/chatbi/results/{id}/charts | 切换图表 | 201 |
| GET /api/chatbi/reports/{id} | 报告 | 200 |
| POST /api/chatbi/runs/{id}/feedback | 反馈 | 201 |
| DELETE /api/chatbi/conversations/{id} | 当前用户软删除 | 204 |
| DELETE /api/chatbi/results/{id} | 当前用户软删除 | 204 |
| GET /api/chatbi/notifications | 系统内通知 | 200 |
| POST /api/chatbi/notifications/{id}/read | 标记已读 | 204 |

JSON 成功信封为 `{data, request_id}`；错误信封使用 C6。删除和标记已读为幂等操作。列表采用 cursor、limit，默认 20、最大 100；ResultSet 读取不使用列表分页协议。

## 13. 安全与验收

Markdown 禁用原始 HTML、脚本、事件属性和危险 URL 协议。报告图表只使用允许的声明式配置，不接受可执行 JavaScript。所有制品经过后端读取，不暴露文件系统路径和永久下载地址。

验收覆盖：主计划与三个 LocalPlan 展示；乱序和重复事件；权限过滤事件的游标连续；Snapshot 与事件竞态；确认排队和重复响应；取消后迟到事件；部分完成；数字路径失效；图表类型不适用；软删除活动会话；权限撤销后的历史结果读取。
