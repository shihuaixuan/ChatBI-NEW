# C3-分析工具与自动下钻详细设计

## 1. 范围与执行边界

本文定义工具注册、调用协议、确定性分析、Evidence 和自动下钻。主 Agent 与三类子 Agent 使用 C1 的 AgentDecision 发起工具调用。每个调用归属一个业务步骤；工具不修改计划、不委派 Agent、不输出运行状态变更指令。

分析 Agent 在 LocalPlan 内依据 Observation 选择下一步。工具内部的查询、计算和校验是确定性流程，不建立独立 ReAct 会话或执行 DAG。decision_id 标识同轮调用集合，ToolCall 标识一次业务调用，ToolAttempt 标识技术重试。

## 2. 工具注册

### 2.1 ToolDefinition

字段：name、version、description、input_schema_ref、output_schema_ref、allowed_roles、allowed_phases、execution_pool、read_only、idempotency_policy、retry_policy、resource_policy_ref、capability_requirements。

execution_pool 为 QUERY、ANALYSIS、DELIVERY。数据库只读不等于工具无状态：结果和审计可持久化，但不能修改业务数据源。工具版本锁定在 RuntimeSnapshot；Schema 与实现版本一起注册，启动时检查函数重名、引用存在和输入输出兼容。

### 2.2 工具目录

所有输入均由 `{step_id, input}` 包装。下表为 input 的正式结构，查询内部结构由 C2 定义。查询、分析结果引用只能来自当前步骤可见集合。

| 工具 | input | output | 角色 |
| --- | --- | --- | --- |
| search_semantic_assets | query、asset_types、known_context_id? | SemanticContext | MAIN、QUERY_AGENT、ANALYSIS_AGENT |
| get_semantic_definition | asset_keys、context_id | SemanticFactResult、Evidence | MAIN、QUERY_AGENT |
| query_metric | intent: QueryIntent | QueryToolOutput | MAIN、QUERY_AGENT、ANALYSIS_AGENT |
| query_detail | intent: QueryIntent | QueryToolOutput | MAIN、QUERY_AGENT |
| read_result | result_id、projection: METADATA / ALL_SAVED_ROWS / ANALYSIS | ResultProjection | 全部 |
| compare_periods | current_result_id、comparison_result_id、metric_keys、alignment_policy_ref | PeriodComparisonResult | MAIN、ANALYSIS_AGENT |
| analyze_trend | result_id、metric_key、time_key、profile_ref | TrendAnalysisResult | MAIN、ANALYSIS_AGENT |
| analyze_ranking_share | result_id、metric_key、dimension_keys、top_n?、denominator_result_id? | RankingShareResult | MAIN、ANALYSIS_AGENT |
| detect_anomaly | result_id、metric_key、time_key、profile_ref | AnomalyAnalysisResult | MAIN、ANALYSIS_AGENT |
| analyze_dimension_contribution | current_result_id、comparison_result_id、current_total_id、comparison_total_id、analysis_contract_ref、dimension_key | AttributionResult | MAIN、ANALYSIS_AGENT |
| analyze_metric_formula | analysis_contract_ref、input_result_ids | AttributionResult | MAIN、ANALYSIS_AGENT |
| analyze_metric_drivers | analysis_contract_ref、input_result_ids | DriverAnalysisResult | MAIN、ANALYSIS_AGENT |
| get_drilldown_candidates | source_analysis_id、session_id? | DrilldownCandidateSet | MAIN、ANALYSIS_AGENT |
| explore_drilldown | candidate_set_id、candidate_id、expected_session_version | DrilldownStepResult | MAIN、ANALYSIS_AGENT |
| evidence_create | statement_template、source_evidence_ids、numeric_bindings | Evidence | MAIN、ANALYSIS_AGENT、REPORT_AGENT |
| build_chart | result_id、chart_type、field_bindings | ChartSpec | MAIN、ANALYSIS_AGENT、REPORT_AGENT |
| build_report | title、blocks、source_result_ids、source_evidence_ids | ReportDocument | MAIN、REPORT_AGENT |

report 工具只处理当前 Run 结果。新建报告 Run 的基础查询由 MAIN 或 QUERY_AGENT 完成；REPORT_AGENT 仅组织已查询结果。最终回答通过 C1.finish_run 提交，不注册另一种结束函数。

## 3. ToolCall 与结果协议

### 3.1 ToolInvocationContext

字段：run_id、agent_id、step_id、decision_id、tool_call_id、attempt_id、generation、question_context_revision、contract_version_id、permission_snapshot_id、data_snapshot_id、runtime_snapshot_id、trace_context。全部由服务端提供。

### 3.2 ToolResult

```json
{
  "tool_result_id":"tr_1",
  "tool_call_id":"tc_1",
  "status":"SUCCEEDED",
  "output":{"type":"QUERY","result_ids":["rs_1"]},
  "evidence_ids":["ev_1"],
  "limitations":[],
  "error":null,
  "usage":{"query_count":1,"analysis_count":0,"elapsed_ms":320}
}
```

output 按 QUERY、SEMANTIC、ANALYSIS、RESULT_PROJECTION、CANDIDATES、DRILLDOWN、EVIDENCE、CHART、REPORT 判别联合，字段由工具输出 Schema 固定。status 为 SUCCEEDED / FAILED；待确认是 ToolCall 的等待状态，不提交伪成功结果。FAILED 必须有 PublicError。

`Observation = {observation_id, run_id, agent_id, step_id, source_type, source_id, tool_result_id?, subtask_result_id?, control_payload?, created_at}`。source_type 为 TOOL_RESULT、SUBTASK_RESULT、CONFIRMATION_RESULT、DECISION_ERROR。唯一键 `(agent_id, source_type, source_id)` 防止重复唤醒。

ToolResult 的输出引用、错误和限制完整保存；大表通过 ResultSet 引用读取，不复制到模型上下文。

### 3.3 校验与提交

```text
工具版本与角色 → 输入 Schema → 所属步骤和可见引用
→ 用户需求约束 → 语义能力 → 权限 → 数据快照
→ 同批独立性 → 配额预留 → 幂等检查 → 派发
```

不存在的引用、其他 Run 的数据引用和同批尚未产生的结果引用均拒绝。失败返回明确错误，不能修改参数后继续执行。正式结果提交遵循 C1 的代次、租约、状态和事务约束。

多个 explore_drilldown 可并行计算，但不能分别修改共享会话。各调用产出独立探测结果；同一候选集全部调用终态后，由服务端以 session_version 条件更新一次会话。版本冲突重新汇总已提交结果，不重新执行查询。

## 4. ResultSet 与完整性

ResultSet 字段：result_id、run_id、agent_id、step_id、tool_call_id、query_execution_id?、kind、schema、row_count、completeness、limit_reason?、artifact_id、content_fingerprint、contract_version_id、permission_snapshot_id、data_snapshot_id、question_context_revision、lineage、created_at。

kind 为 QUERY / COMPUTED。列 Schema 包含 key、label、data_type、semantic_role、asset_key?、unit?、scale?、nullable。Decimal 在 JSON 中使用字符串，不能先转浮点数再序列化。

| completeness | 定义 | 分析使用 |
| --- | --- | --- |
| COMPLETE | 完整覆盖查询要求的总体或数据库内全量聚合结果 | 可用于对应总体分析 |
| LIMITED_BY_USER | 数据库按用户 Top N 等要求形成的受限集合 | 仅用于该集合；总体分母必须另查 |
| TRUNCATED_BY_SYSTEM | 资源上限导致缺失 | 不用于总体占比、完整排名、异常或归因 |

行数小于上限不自动证明完整。明细最多 200 行、聚合最多 1000 行；高基数分析在数据源内聚合为完整小结果，不能先截断再计算。“前十名占总体比例”必须使用同快照完整总额作为分母。

## 5. 数值与对齐

Decimal 运算精度为 38 位，内部 ROUND_HALF_EVEN，展示精度读取指标 format_policy。数据源数值类型超出范围返回 NUMERIC_RANGE_EXCEEDED。NULL、缺失和零值分别处理，不相互替换。

`NumericValue = {value: decimal_string?, status: VALID | UNDEFINED | MISSING, reason?}`。除零返回 UNDEFINED + DIVISION_BY_ZERO；零分母时不能输出无穷大。排序最后放置 UNDEFINED / MISSING，并保留状态。

对齐键由维度标准值、时间桶与实体键组成，使用类型化规范值。两期键全集做 full alignment；缺失成员是否补零由发布的 missing_member_policy 决定。禁止仅按展示标签连接。

## 6. 标准分析

### 6.1 同比环比

```text
change = current - baseline
change_rate = change / abs(baseline), baseline != 0
```

change_rate 表示有符号变化率；分母为零返回 UNDEFINED。两期使用相同指标定义、筛选、粒度、快照和时间对齐策略。输出每个对齐键的 current、baseline、change、change_rate、comparison_kind 和 limitations。

### 6.2 趋势

输入按时间桶升序，重复桶拒绝。输出首末值、绝对变化、有效区间变化率、最大值与最小值及对应时间。移动平均使用发布 profile 的 window，历史不足返回 INSUFFICIENT_HISTORY，不改变窗口。缺失桶按日历补全，值仍为 MISSING；只有指标 zero_fill_policy 明确允许才补零。

### 6.3 排名占比

排名按数值、发布次序和维度键稳定排序。并列名次使用 DENSE_RANK。share = member_value / total；total 为零时 UNDEFINED。负值指标只有发布 share_policy 允许时才能计算占比，否则返回 SHARE_DOMAIN_UNSUPPORTED。

Top N 之外的 OTHER 必须由完整总体减去保留成员或由数据库完整聚合产生。OTHER 是汇总桶，不作为实体继续下钻。

## 7. 异常检测

profile 固定 detector、window、minimum_history、threshold、zero_mad_policy、missing_policy 和 version。模型只能选择已发布 profile_ref，不能提交阈值或算法代码。

### 7.1 ROBUST_Z_SCORE

```text
m = median(values)
MAD = median(abs(value_i - m))
score_i = 0.67448975 * (value_i - m) / MAD
```

默认 threshold=3.5，minimum_history=14。MAD=0 且所有值相同，输出 NO_ANOMALY；MAD=0 但存在偏离值，输出 UNDEFINED_SCORE，并以发布的绝对偏离阈值判断。未配置绝对阈值时返回 ANOMALY_PROFILE_INCOMPLETE，不临时切换算法。

### 7.2 ROLLING_DEVIATION

baseline_t 为前 window 个有效点中位数，不包含当前点；relative_deviation = (actual-baseline)/abs(baseline)。基线为零时只使用 profile 明确配置的绝对偏离阈值。默认 window=7、minimum_history=14；阈值必须在发布配置中给出。

### 7.3 SEASONAL_BASELINE 与 CHANGE_POINT

SEASONAL_BASELINE 使用发布日历映射的前 k 个同周期位置的中位数，k 与最小有效样本数由 profile 指定，不用相邻点替代缺失同期。

CHANGE_POINT 采用固定左右窗口的均值差检测：对每个候选切点计算 left_mean、right_mean、difference，以 profile.absolute_threshold 判定。窗口不得重叠；边界不足一个完整窗口不计算。连续命中点按 profile.merge_distance 合并，保留绝对变化最大的切点，平局保留较早点。

多检测器 profile 必须规定 min_agreement；采用按时间点投票，不对不同量纲的原始 score 直接加权。有效检测器不足 min_agreement 时返回 INSUFFICIENT_DETECTORS，不自动降低门槛。统计异常不等于业务因果结论。

## 8. 维度贡献

前提：指标可加、维度成员互斥且覆盖总体、两期完整、同口径和同快照，合约声明 PERIOD_DELTA_CONTRIBUTION。

```text
contribution_i = current_i - baseline_i
D = current_total - baseline_total
R = D - sum(contribution_i)
contribution_rate_i = contribution_i / D, D != 0
absolute_impact_share_i = abs(contribution_i) / sum(abs(contribution_j))
```

D=0 时 contribution_rate 为 UNDEFINED；绝对变化合计为零时 absolute_impact_share 为 UNDEFINED。贡献率可大于 1 或为负，不强制裁剪。

对账条件为 `abs(R) <= max(absolute_tolerance, relative_tolerance * abs(D))`。两个容差必须有单位并存入 analysis_contract。coverage 在 D 非零时为 `max(0, min(1, 1-abs(R)/abs(D)))`；D=0 时使用对账结果，不计算覆盖率。

高基数场景先按同一成员全集计算两期差额，再按绝对贡献降序选择成员，其他成员汇总为 OTHER；不能分别取两期 Top N 后求差。正负贡献分别展示。跨维度分解结果不得相加计算总体覆盖率，因为其成员集合重叠。

## 9. 公式分解与驱动

方法由 analysis_contract 指定。定义域不成立返回 OUT_OF_DOMAIN，不能自动换算法。

### 9.1 DIRECT_DELTA

对于 `Y = c + Σ(a_i * X_i)`，`contribution_i = a_i * (X_i1-X_i0)`。固定常数贡献为零。必须使用发布公式 AST 中的系数。

### 9.2 MULTIPLICATIVE_LOG_DECOMPOSITION

对于正值乘法 `Y = ΠX_i`：

```text
L(Y1,Y0) = (Y1-Y0)/(ln(Y1)-ln(Y0)), Y1 != Y0
L(Y1,Y0) = Y1, Y1 == Y0
contribution_i = L(Y1,Y0) * ln(X_i1/X_i0)
```

所有输入和结果必须为正。对数运算使用固定 Decimal 精度；贡献合计按容差对账。

### 9.3 SHAPLEY_FORMULA_DECOMPOSITION

对所有输入替换排列计算每个输入的边际变化，再求平均。最多 6 个可变输入，使用精确排列；公式每个混合输入组合都必须在定义域内。任一组合除零或非法，整体返回 OUT_OF_DOMAIN。超过输入上限返回 FORMULA_INPUT_LIMIT_EXCEEDED，不随机采样。

### 9.4 RATIO_DECOMPOSITION

对于 `Y=N/D`：

```text
numerator_effect = 0.5 * [(N1-N0)/D0 + (N1-N0)/D1]
denominator_effect = 0.5 * [(N0/D1-N0/D0) + (N1/D1-N1/D0)]
```

D0、D1 均非零。两项和与 Y1-Y0 对账。

### 9.5 SHARE_CHANGE_DECOMPOSITION

对于 `Y=Σ(w_i*r_i)`：

```text
rate_effect_i = 0.5*(w_i1+w_i0)*(r_i1-r_i0)
structure_effect_i = 0.5*(r_i1+r_i0)*(w_i1-w_i0)
```

每期权重和必须为 1。成员新增或消失时缺失期权重为零；该期 rate 使用另一期间的 rate 作为分解约定，使变化全部计入结构项，并记录 MEMBER_RATE_CARRIED_FOR_DECOMPOSITION。两期均无有效 rate 时拒绝分解。

### 9.6 驱动分析

驱动图只读取发布态 metric_dependency。按固定边展开，去除循环和重复查询。驱动边若无可计算分解关系，只输出关联性变化，不生成可加贡献。不同层级贡献不直接求和；父因素和其子因素属于同一贡献的不同分解。

## 10. 自动下钻

### 10.1 对象

DrilldownSession 字段：session_id、run_id、agent_id、owner_step_id、root_metric_key、baseline_ref、version、status、automatic_depth_limit、confirmed_depth_limit、hard_depth_limit、visited_keys、frontier、selected_path_refs、budget_usage。

status 为 RUNNING、WAITING_CONFIRMATION、COMPLETED、STOPPED、FAILED。根指标为第 0 层，每沿一条发布的维度层级或驱动边推进增加 1。默认自动到第 3 层；进入第 4 层确认，确认最多授权第 5 层。

DrilldownCandidate 字段：candidate_id、candidate_set_id、session_id、session_version、source_ref、target_ref、direction_type、path_ref、next_depth、semantic_priority、estimated_member_count、required_filters、requires_confirmation、exclusion_reasons。direction_type 为 DIMENSION_CONTRIBUTION、DIMENSION_LEVEL、FORMULA_INPUT、DRIVER_METRIC。

### 10.2 候选生成与执行

候选生成顺序：读取 analysis_contract → 展开合法边 → 检查已访问键和环 → 权限及能力过滤 → 缺失输入与成本检查 → 稳定排序。访问键包含指标、维度、成员筛选、时间范围、路径和快照，不能只用维度名去重。

排序依次为语义优先级升序、已知绝对贡献降序、预计成本升序、candidate_id 升序。Agent 从返回的合法候选中选择，同轮最多 3 个方向。explore_drilldown 内部按冻结候选执行受控查询和确定性分析，不能自行选择其他方向。

### 10.3 停止

无候选、达到深度上限、用户拒绝、资源预算耗尽、连续两层无新增显著证据、权限撤销或快照失效均停止。显著性阈值来自 analysis_contract，不由模型提供。

覆盖率只在同一可对账分解内比较。`improved` 表示同一分解的残差减少达到阈值，或新增达到显著性阈值的事实；不同维度的覆盖率不能累加。停止原因进入 DrilldownStepResult、Evidence 和最终限制。

Agent 通过 update_plan 增加业务步骤或 finish_subtask 结束任务；工具不直接创建计划节点。

## 11. Evidence 与来源校验

Evidence 字段：evidence_id、run_id、agent_id、step_id、type、source_refs、facts、limitations、contract_version_id、permission_snapshot_id、data_snapshot_id?、fingerprint。

facts 为命名事实集合，每项包含 value、value_type、unit、source_ref、source_path。数字绑定只能引用 facts 中存在且类型兼容的路径。SYNTHESIS 仅组合来源事实，不执行计算；新计算必须通过分析工具产生 COMPUTED ResultSet 与 CALCULATION Evidence。

SEMANTIC_FACT 来源是固定发布版本，可没有 data_snapshot_id。数据事实必须具有有效快照和查询血缘。失败或迟到结果不得生成正式 Evidence。

## 12. 错误、资源与验收

错误码：TOOL_NOT_ALLOWED、TOOL_INPUT_INVALID、REFERENCE_NOT_VISIBLE、INCOMPLETE_RESULT、ALIGNMENT_MISMATCH、OUT_OF_DOMAIN、RECONCILIATION_FAILED、DRILLDOWN_CANDIDATE_EXPIRED、RESOURCE_LIMIT_EXCEEDED。空结果、无异常和未发现显著贡献是成功结果，必须明确业务状态。

查询数、分析次数、下钻深度、输出字节数和并发预算分别计数；子任务共享 Run 总预算。相同查询可在同 Run 指纹一致时复用，不能因同名指标复用不同筛选结果。

验收包括：正负抵消、零分母、缺失成员、两期不同 Top N、OTHER 对账、多维度重复覆盖、Shapley 混合输入除零、异常 MAD 为零、季节数据不足、下钻第 4 层确认、并行候选会话版本冲突及重复调用只提交一个正式结果。

## 13. 工具输出字段

所有分析输出包含 analysis_result_id、run_id、analysis_type、algorithm_version、input_refs、contract_version_id、data_snapshot_id、limitations、fingerprint。下表定义各类型专有字段，不允许工具返回未注册结构。

| 类型 | 专有字段 |
| --- | --- |
| QueryToolOutput | result_ids: string[]、result_roles: {result_id, role}[]、query_ids: string[]；role 为 CURRENT / BASELINE / DETAIL / TOTAL |
| SemanticFactResult | asset_keys、definitions: {asset_key, definition, contract_version_id}[]、evidence_ids |
| PeriodComparisonResult | comparison_kind、alignment_policy_ref、items: {keys, current, baseline, change, change_rate}[] |
| TrendAnalysisResult | time_key、metric_key、points、first、last、change、change_rate、maximum、minimum、moving_averages |
| RankingShareResult | metric_key、items: {keys, rank, value, share}[]、total、other?、population_scope |
| AnomalyAnalysisResult | profile_ref、status: ANOMALY_FOUND / NO_ANOMALY、events: {time, actual, baseline, difference, detector_results}[] |
| AttributionResult | method、target_metric_key、total_change、items: {subject_ref, contribution, contribution_rate, absolute_impact_share}[]、residual、coverage、reconciliation_status |
| DriverAnalysisResult | target_metric_key、driver_items: {metric_key, current, baseline, change, relation_type, evidence_id}[]、decomposition_result_ids |
| DrilldownStepResult | session_id、candidate_set_id、candidate_id、depth、analysis_result_ids、evidence_ids、improved、stop_reason?、next_frontier |
| ResultProjection | result_id、projection、schema、rows?、analysis?、completeness、content_fingerprint |

所有数值位置使用 NumericValue；keys 使用列 Schema 对应的类型化值。population_scope 为 FULL / USER_SELECTION，不允许用截断结果填充 FULL。reconciliation_status 为 PASSED / FAILED / NOT_APPLICABLE。

ResultProjection 的 METADATA 仅含字段、数量、完整性和指纹；ALL_SAVED_ROWS 包含全部已保存行；ANALYSIS 包含完整已保存分析对象。三种投影都不能改变源对象指纹或隐去其限制。
