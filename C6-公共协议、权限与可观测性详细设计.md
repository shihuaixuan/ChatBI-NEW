# C6-公共协议、权限与可观测性详细设计

## 1. 范围与协议归属

本文定义公共标量、身份、权限、可见性、错误、审计、日志、指标和 Trace。业务 DTO 由唯一所属文档维护：C1 定义运行和计划；C2 定义理解和查询；C3 定义工具、ResultSet 和 Evidence；C4 定义用户视图；C5 定义模型调用；A 定义语义资产；D 定义物理存储。

同一对象不在多个模块分别维护字段副本。Pydantic 模型生成 JSON Schema，REST 生成 OpenAPI，工具参数直接引用注册 Schema。文档中的结构表定义字段语义，生成物用于实现与契约测试。

## 2. 公共类型

| 类型 | 规范 |
| --- | --- |
| ExternalId | 带对象前缀的不可变字符串；内部 bigint 不直接作为外部对象引用 |
| Timestamp | RFC 3339，存储 UTC；展示按 Run.timezone |
| DecimalString | 十进制字符串；禁止 NaN、Infinity 和二进制浮点中间转换 |
| Ref | `{type, id, version?}`；type 为注册对象类型 |
| Fingerprint | SHA-256，输入为规范化 JSON；对象键排序、数组语义顺序保留、Decimal 规范化 |
| JsonValue | JSON 标量、数组或对象；只用于有 Schema 约束的扩展载荷 |
| ProtocolRef | `{name, major, minor}` |
| TraceContext | `{trace_id, span_id, parent_span_id?, request_id}` |

所有输入 DTO 使用额外字段禁止策略。nullable 与可省略不同：必填 nullable 字段必须出现。字段名称采用 snake_case。枚举为封闭集合；新增状态或事件类型属于需要兼容处理的协议变更，不能默认视为可忽略字段。

minor 版本只增加有默认值的可选字段；删除字段、改变类型、增加接收方必须处理的枚举需要 major 版本变更。保存历史对象的 schema_version，历史解码器不能用新默认值改写旧事实。

## 3. 公共命令与幂等

CommandEnvelope 字段：command_id、command_type、target_ref、expected_version?、payload、issued_at。ActorContext 由认证中间件生成，不接受客户端 payload 指定 user_id、oid 或权限。

幂等唯一键为 `(oid, actor_id, command_type, command_id)`。命令保存请求指纹、状态和结果引用；重复同指纹返回原结果，不同指纹返回 IDEMPOTENCY_CONFLICT。跨模块状态变更使用公开命令，不直接写入他方表。

## 4. 身份与权限能力

ActorContext 字段：oid、actor_id、actor_type、user_id?、service_id?、request_id。actor_type 为 USER / SERVICE。ViewerContext 增加当前账号状态和可见性策略版本。

| 能力 | 操作 |
| --- | --- |
| chatbi.query.execute | 创建交互或报告 Run 并执行查询 |
| chatbi.result.read | 读取有权访问的结果、证据和图表 |
| chatbi.run.interact | 修改、确认和取消自己的 Run |
| chatbi.report.create | 创建系统内报告 |
| chatbi.monitor.manage | 管理自己的监控任务 |
| semantic.asset.edit | 修改语义草稿 |
| semantic.asset.review | 审核指定草稿版本 |
| semantic.asset.publish | 发布已批准内容 |
| chatbi.diagnostic.read | 读取受限诊断信息和权限 Trace |

资源层级为工作空间、数据集、语义视图、资产、字段和行。拥有功能权限不代表拥有数据权限。管理员诊断能力不自动赋予业务数据读取权。

首期会话和 Run 仅所有者可操作与读取；系统管理员通过独立诊断接口访问受限诊断，不通过普通会话接口绕过所有者条件。共享或外部分发不属于首期接口。

## 5. PermissionSnapshot

字段：permission_snapshot_id、oid、owner_user_id、dataset_id、semantic_view_id、policy_version、permission_fingerprint、allowed_asset_keys、field_policy_artifact_ref、row_policy_artifact_ref、masking_policy_ref、created_at。

Run 准备时校验账号、数据集、语义视图和查询能力，生成不可变快照。行权限、字段隐藏和脱敏规则必须可编译；无法表达的规则返回 PERMISSION_POLICY_UNSUPPORTED，禁止以未过滤查询替代。

执行时：

1. ToolHarness 校验工具与资产权限。
2. 编译器通过 PermissionFacade 取得快照对应的权限逻辑计划片段。
3. 权限过滤在语义逻辑计划内注入，再生成 SQL。
4. QueryExecution 在派发和正式结果提交前复核当前账号与授权有效性。
5. 结果读取使用当前权限再次复核。

权限规则只有编译器一个注入入口，Worker 不拼接或再次注入 SQL 条件。查询产物保存权限指纹与受限审计引用。

## 6. 当前权限复核与撤销

读取对象时依次检查：软删除、所有者、账号、工作空间、数据集、语义视图、资产与字段，以及当前行权限是否覆盖来源对象。

行权限覆盖不能对任意 SQL 谓词做未经证明的等价推断。首期采用以下确定规则：

- 当前权限指纹与来源指纹一致，允许继续字段脱敏投影。
- 不一致时，仅允许权限服务证明为相同规则或有限集合/区间的包含关系。
- 无法证明当前权限覆盖原结果时返回 RESULT_PERMISSION_CHANGED；聚合结果不能通过删行恢复正确权限。

权限缩小、账号停用或授权撤销使活动 Run 进入 FAILED + ACCESS_REVOKED，阻止新派发及正式提交，并隐藏不再可见结果。已发送到客户端的数据无法撤回，服务端终止 SSE 并要求客户端移除对应视图。

权限扩大不修改原快照。用户重新执行创建新 Run，固定新的权限与语义版本。确认按钮不授予权限，不存在通过模型或用户确认覆盖授权校验的路径。

## 7. 可见性与 SQL 投影

VisibilityDescriptor 字段：owner_user_id、dataset_id、required_capabilities、source_permission_snapshot_id、classification、artifact_ref?。classification 为 PUBLIC_METADATA、BUSINESS_DATA、SENSITIVE_DATA、SECURITY_DIAGNOSTIC、SECRET。

| 对象 | 普通所有者 | 诊断授权用户 |
| --- | --- | --- |
| MainPlan、LocalPlan、步骤和子任务 | 可见安全业务内容 | 同样受数据权限约束 |
| ResultSet、Evidence、回答、图表 | 当前权限允许时可见 | 不自动越过数据权限 |
| 编译 SQL | 安全投影可见 | 受限接口可查看诊断版本 |
| 权限来源与注入 Trace | 不可见 | 独立审计后可见 |
| 快照 Token、密钥、连接凭证 | 不可见 | 普通诊断接口也不返回 |
| 原始 CoT | 不采集 | 不采集 |

SQL 普通投影保留语句结构，权限参数使用稳定占位符并隐藏其值；用户筛选参数按当前脱敏规则展示。源权限片段中的敏感常量必须参数化或在 AST 投影中替换，不以正则替换完整 SQL。投影保留 query_execution_id，不能伪装为可直接复现全部权限的 SQL。

所有内容读取经过 VisibilityProjector。ArtifactStore 不提供不带鉴权的物理地址；前端 URL 通过后端按次授权。

## 8. 错误协议

PublicError 字段：code、category、message、retryable、target_ref?、details、trace_id。message 为安全中文说明，不包含堆栈、凭证、SQL 参数或权限表达式。details 按 code 使用封闭 Schema。

ErrorEnvelope：`{error: PublicError, request_id}`。

| category | HTTP | 示例 |
| --- | --- | --- |
| VALIDATION | 422 | TOOL_INPUT_INVALID、TIME_ALIGNMENT_UNSUPPORTED |
| AUTHENTICATION | 401 | AUTHENTICATION_REQUIRED |
| AUTHORIZATION | 403 | ACCESS_REVOKED、RESULT_PERMISSION_CHANGED |
| NOT_FOUND | 404 | RESOURCE_NOT_FOUND |
| CONFLICT | 409 | PLAN_VERSION_CONFLICT、IDEMPOTENCY_CONFLICT |
| EXPIRED | 410 | EVENT_CURSOR_EXPIRED |
| RATE_LIMIT | 429 | USER_CONCURRENCY_LIMIT |
| DEPENDENCY | 503 | MODEL_ROUTE_UNAVAILABLE、DATA_SOURCE_UNAVAILABLE |
| INTERNAL | 500 | INTERNAL_ERROR |

异步工具失败通过持久化 ToolResult.error 返回；Run 失败通过 Run.error 和 RUN_STATUS_CHANGED 返回。SSE 连接建立后不能修改 HTTP 状态，鉴权变化发送安全 stream.control 并关闭连接。

retryable 只表示相同业务输入的技术重试是否可能成功，不表示可以更换模型、版本、权限或参数。

## 9. 审计与事务

AuditRecord 字段：audit_id、oid、actor_id、action、target_ref、run_id?、decision_id?、tool_call_id?、outcome、reason_code?、metadata、trace_id、occurred_at。outcome 为 SUCCEEDED / DENIED / FAILED。

必须审计：Run 创建/取消/终态、计划变更、子任务委派、工具派发与结果提交、确认响应、权限拒绝、诊断读取、语义编辑/审核/发布、软删除。

业务状态和审计在同一 PostgreSQL 事务提交。外部调用先持久化 Attempt；外部成功而本地提交失败时由恢复流程处理，不能补写虚假成功。制品读取涉及解密诊断内容时，先提交访问审计再返回正文。

普通 metadata 只含 ID、版本、指纹、耗时、行数、用量和安全错误码；完整 Prompt、数据行、权限规则及凭证不得写入普通审计字段。

## 10. 日志、指标与 Trace

日志字段：timestamp、level、service、request_id、trace_id、run_id、agent_id、plan_id、step_id、decision_id、tool_call_id、attempt_id、subtask_id、error_code、duration_ms。日志记录安全摘要，完整诊断内容以加密制品引用保存。

指标：Run 终态数量与耗时、等待额度时长、模型调用延迟与 Token、工具失败率、查询耗时、语义歧义率、完成校验失败率、PlanPatch 次数、租约过期次数、快照失效率、SSE 补发延迟。

指标标签只使用 role、tool_name、status、error_category、datasource_type 等低基数值；run_id、user_id、query_id 不作为时序指标标签。

Trace 层级为 Run → AgentDecision → ModelInvocation / ToolCall → QueryExecution / Analysis；子任务通过 parent_step_id 与 parent_span_id 关联。跨进程使用 TraceContext 传播，不把模型输入作为 Span 属性。

## 11. 制品、保留与配置

Artifact 元数据含 artifact_id、classification、content_hash、size_bytes、schema_version、storage_key、status、expires_at?。状态为 STAGED / AVAILABLE / DELETING / DELETED。内容不可变，完整性不匹配返回 ARTIFACT_INTEGRITY_FAILED。

默认保留：业务结果与报告 90 天，事件 30 天，受限模型诊断 7 天，运行元数据和业务审计 365 天。管理员可配置更长保留期；已被有效对象引用的制品不能先于其引用对象删除。软删除不立即清除审计。

可选诊断、遥测和数据源适配器使用惰性加载；启用但依赖不存在时抛出明确 ImportError 或配置错误，禁止将导入失败对象赋值为 None 并延迟产生不明确异常。

## 12. 验收

契约测试覆盖额外字段、版本兼容、未知枚举、幂等键冲突、跨工作空间引用和对象所有者校验。权限测试覆盖字段隐藏、行权限、聚合结果权限变化、SQL 参数投影、撤销期间提交和诊断访问审计。可观测性测试检查日志无凭证、指标无高基数标识、Trace 跨进程关联完整、审计与状态同时提交。

## 13. 权限服务接口

AccessCheckRequest 字段：actor_context、operation、resource_ref、run_id?、permission_snapshot_id?、required_asset_keys、source_permission_fingerprint?。operation 为 EXECUTE、READ、INTERACT、PUBLISH、DIAGNOSE。

AccessDecision 字段：allowed、reason_code、current_policy_version、coverage_status、visibility_descriptor?、audit_ref。coverage_status 为 SAME、PROVEN_SUPERSET、NOT_COVERED、UNPROVEN；只有 allowed=true 且覆盖成立才可读取已有结果。

PermissionFacade 提供 check、check_batch、create_snapshot、get_compilation_policy、project_sql。check_batch 对每个资源返回独立 AccessDecision，不以一个资源获批推断同批全部获批。get_compilation_policy 只向可信编译服务提供结构化权限片段；客户端和模型不调用该接口。

相同请求的短期权限缓存必须含当前 policy_version。撤销事件使相关缓存失效，正式派发、结果提交和读取仍取得可验证的当前策略版本。缓存服务不可用时直接查询权威权限服务；权威服务不可用时拒绝受保护操作，不按旧缓存授权。
