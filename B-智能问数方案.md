# B-智能问数方案

## 1. 目标与技术基线

系统提供基于发布语义层的企业智能问数。执行架构固定为主 Agent、QUERY_AGENT / ANALYSIS_AGENT / REPORT_AGENT 三类子智能体及高层业务计划。模型负责规划和工具决策，程序负责权限、语义绑定、查询编译、确定性计算、状态和证据校验。

技术基线为 Python 3.11、FastAPI、Pydantic 2、PostgreSQL、Redis、REST + SSE 和不可变 ArtifactStore。本文规定实施范围、工作包、依赖、交付物和验收；总体架构见 C，详细协议按 A、C1～C6、D 的唯一归属维护。

## 2. 交付范围

| 能力 | 交付内容 |
| --- | --- |
| 语义管理 | 资产编辑、校验、审核、发布、版本读取、检索与分析合约 |
| 问题理解 | 多轮指代、需求与约束、语义歧义、时间和值绑定 |
| 运行时 | 主/子 Agent、业务计划、工具决策、等待、确认、取消、恢复 |
| 数据查询 | 只读确定性编译、权限注入、数据快照、完整性标记 |
| 标准分析 | 对比、趋势、排名占比、异常、贡献、公式与驱动 |
| 自动下钻 | 发布候选、最多五层、第 4 层确认、预算与停止条件 |
| 结果交付 | Evidence、数字绑定回答、图表、系统内报告、SSE 与反馈 |
| 公共能力 | 身份、权限、错误、审计、Trace、资源配额和保留清理 |

不提供模型自由 SQL、任意代码工具、跨数据源联合查询、子任务递归委派或报告外部分发。未通过语义与快照能力验收的数据源不得启用正式分析。

## 3. 业务不变量

1. 一个 Run 固定一个数据集、数据源、语义视图、语义版本和数据快照。
2. MainPlan / LocalPlan 只表达业务任务、验收条件和依赖，工具选择通过 AgentDecision 完成。
3. 每个业务产物归属步骤和调用；每个终态步骤只有一个不可变 StepResult。
4. 语义、权限、数据版本及不可变筛选不能由模型修改。
5. 数值只来自查询或确定性工具；最终回答绑定有效 Evidence。
6. 计划、调用、结果、事件和审计具有明确事务边界；过期执行者不能提交正式结果。
7. 用户确认只恢复合法操作，不扩大权限或突破硬限制。
8. 新 Run 重新查询，历史结果只用于理解。

## 4. 工作包与并行依赖

| 工作包 | 主要交付物 | 依赖 | 独立验收 |
| --- | --- | --- | --- |
| P0 公共契约 | 公共类型、DTO Schema、错误表、事件目录、数据库迁移定义 | 无 | Schema 和示例一致、跨对象引用规则 |
| P1 语义层 | 编辑发布、检索、分析合约、查询编译器 | P0 | 固定数据集的 SQL 与结果正确性 |
| P2 Agent 运行时 | Run、Agent、Plan、Decision、子任务、等待、恢复 | P0 | 使用模型与工具 stub 验证状态机 |
| P3 模型服务 | Provider 适配、四类 Agent 任务、语言任务、提示词与预算 | P0 | 原生工具调用、结构校验、重试与取消 |
| P4 查询执行 | 绑定、时间和值解析、权限编译、快照与 ResultSet | P0、P1 接口 | 数据变更期间一致性、只读和完整性 |
| P5 分析工具 | 对比、趋势、异常、贡献、公式、驱动与下钻 | P0、P1 分析合约、P4 ResultSet | 数值样例和对账 |
| P6 交付与前端 | 会话、主/局部计划、确认、SSE、图表、回答和报告 | P0 | 固定事件序列与快照归并 |
| P7 集成与运维 | 部署、配额、故障恢复、压测、清理 | P1～P6 | 完整流程和故障验收 |

P0 完成后 P1、P2、P3、P6 可并行；P4、P5 可用固定契约测试替身提前开发，正式验收依赖真实语义编译与查询。数据库迁移由统一负责人维护，业务团队不能分别建立不同 Plan、ResultSet 或权限表。

## 5. 契约冻结条件

进入并行开发前必须交付：

- Pydantic DTO 与生成的 JSON Schema，包含必填、可空、封闭枚举和额外字段策略。
- C1 状态迁移与控制函数 Schema；C3 工具输入输出 Schema。
- C4 OpenAPI、SSE 事件载荷与 RunViewSnapshot 示例。
- D 的表、唯一键、外键、索引与事务映射。
- 可重复使用的语义发布版本、数据样本、模型 stub、工具结果和事件序列。

契约变更必须同时更新权威定义、生成物、生产者、消费者和对应验收样例。未更新消费者的破坏性变更不得合入集成版本。

## 6. 实施阶段

### 6.1 M0：公共契约与存储

实现 P0、身份上下文、权限快照、ArtifactStore、事件和审计。建立 Run、Agent、Plan、ToolCall、子任务的存储模型。业务模型输出使用 stub，禁止以任意 JSON 临时代替正式协议。

退出条件：重复命令幂等；同 Run 多 LocalPlan 可存储；事件与状态原子提交；所有对象能够按固定 Schema 编解码。

### 6.2 M1：单指标完整流程

完成会话 → QuestionContext → MainPlan → query_metric → 绑定与编译 → 同一快照查询 → Evidence → finish_run → SSE。数据源先验收一种已定义快照适配模式。

退出条件：查询结果与标准 SQL 一致；无来源数字拒绝；取消和过期 Worker 无正式结果；语义歧义暂停；权限条件不可删除；重连后结果不重复。

### 6.3 M2：子任务与标准分析

实现三类子任务的创建、LocalPlan、等待与结果回传，加入同比环比、趋势、排名占比、图表和多轮实体追问。

退出条件：主 Agent 不读取未交付子任务内部结果；ALL/ANY/REQUIRED 对失败目标正确唤醒；用户 Top N 不冒充总体；新 Run 不复用旧数据证据；报告角色不能查询。

### 6.4 M3：异常、归因与自动下钻

实现发布分析合约、确定性算法、候选生成、第 4 层确认、预算和停止规则。下钻动态选择由 ANALYSIS_AGENT 决定，工具内部不维护第二种 Agent 循环。

退出条件：贡献与总变化对账；跨维度覆盖率不相加；公式定义域完整验证；重复候选只执行一次；并行结果合并不覆盖会话版本；第 5 层后不再派发查询。

### 6.5 M4：报告、监控与运行验收

完成数字绑定报告、反馈、软删除、低优先级监控和系统内通知；执行故障测试、权限回归、性能与保留清理验证。

退出条件：所有正式功能达到第 7～9 章验收要求，未通过能力在注册表中禁用，不能通过备用流程静默放行。

## 7. 功能验收数据集

固定数据包含两个期间、多个渠道、缺失成员、零分母、负变化、重复事实关系、高基数成员、权限分区和异常时间序列。发布合约与预期结果一起版本化。

| 用例 | 必须验证 |
| --- | --- |
| 单指标及分组 | 绑定、SQL、参数、数值与来源一致 |
| 同名指标 | 必须澄清，不按最高分直接执行 |
| 未发布字段 | 明确拒绝，无物理字段替代 |
| 同比环比 | 日历、口径和快照相同 |
| Top N 与占比 | 排名总体完整、分母独立且同快照 |
| 贡献 | 正负抵消、OTHER、残差与容差 |
| 公式 | Decimal、除零、Shapley 全组合定义域 |
| 下钻 | 发布路径、重复上下文、确认与硬上限 |
| 历史指代 | 有界实体键重新查询，旧 Evidence 不复用 |
| 报告 | 当前 Run 来源、数字绑定、权限和角色 |

查询与计算正确性用确定性断言验收；真实模型评测使用单独问题集，记录理解准确率、工具选择成功率、需求覆盖率、修正次数、延迟与 Token。禁止只按回答语言流畅度判定成功。

## 8. 可靠性与安全验收

故障注入点：模型返回前、工具执行后提交前、事务提交后 Redis 通知前、子任务完成后主 Agent 唤醒前、SSE 历史补发与实时切换时、快照持有进程失联时。

必须满足：

- 每个业务调用最多一个正式 ToolResult，每步最多一个 StepResult。
- 过期 generation 的输出只审计，不成为 Evidence。
- Redis 中断后 PostgreSQL 扫描继续推进。
- 确认重复响应不重复执行；同键不同响应返回冲突。
- 取消 300 秒内进入 CANCELLED，外部迟到响应不改变终态。
- 权限撤销后不能派发、正式提交或读取无权结果。
- 快照失效不拼接新数据，重跑创建新 Run。
- Prompt、凭证、权限表达式和原始数据不进入普通日志。

## 9. 性能验收

使用固定硬件与依赖配置记录测试报告。容量目标为 50 在线用户、100 活动 Run、每用户最多 5 个活动 Run。压测同时覆盖简单查询、子任务并行和待确认场景。

内部链路验收目标：创建命令接口 p95 ≤ 500 ms；事件提交至 SSE 可读 p95 ≤ 1 s；存在空闲额度时触发器至领取 p95 ≤ 1 s。上述目标不包含模型及业务数据源执行时间，外部耗时单独统计。

端到端报告必须列出各类问题 p50/p95、模型调用次数、Token、查询数、数据源连接数和失败率。未确定真实模型与数据源性能前，不声明统一端到端秒级 SLA。

## 10. 发布条件

发布版本必须同时包含协议生成物、数据库迁移、固定提示词版本、工具注册表、语义能力版本及验收报告。迁移先验证空库建表、历史版本读取和回滚兼容；不可变结果不因升级重写。

功能启用以契约、数值、权限和故障验收全部通过为条件。架构、Schema、数据库和前端事件必须来自同一发布基线，不允许只升级单个冲突文档对应的模块。
