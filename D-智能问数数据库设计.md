# D-智能问数数据库设计

## 1. 范围与存储约定

本文定义 C1 架构的 PostgreSQL 目标存储。运行、模型决策、工具调用、子任务与业务计划独立建模；不使用工具执行节点表代替业务 PlanStep。语义资产遵循 A。

内部主键统一 `id bigint generated by default as identity`；外部 ID 为 `varchar(64)` 且唯一。时间使用 timestamptz。工作空间字段为 oid bigint。可变状态表包含 lock_version bigint not null default 0。JSONB 必须对应版本化 Schema，不替代需要查询、约束或关联的独立字段。

下文字段后 `?` 表示可空；未标注均为 NOT NULL。所有实体表具有 id、created_at，状态表另有 updated_at；包含外部 xxx_id 的表均建立该列唯一约束。文中 FK 简写对应 bigint 外键。外部引用先按 oid 解析为内部 ID，再建立外键。业务正文和大结果存放 ArtifactStore，数据库保存引用、大小和指纹。

## 2. 会话、命令与 Run

### 2.1 chatbi_conversation

字段：conversation_id varchar(64)、oid bigint、owner_user_id bigint、dataset_id bigint、semantic_view_id varchar(128)、title varchar(256)、lock_version bigint。

索引：unique(conversation_id)；(oid, owner_user_id, updated_at desc)。会话数据集创建后不可修改。

### 2.2 chatbi_message

字段：message_id varchar(64)、conversation_id bigint FK、role varchar(16)、content text、parent_run_id bigint?、run_id bigint?、command_id varchar(128)。role 为 USER / ASSISTANT / SYSTEM。

约束：unique(message_id)、unique(conversation_id, command_id, role)。消息保存原文，运行时解释存入 QuestionContext。

### 2.3 chatbi_command

字段：command_id varchar(128)、oid bigint、actor_id varchar(64)、command_type varchar(64)、target_type varchar(32)、target_id varchar(64)、request_fingerprint varchar(80)、payload jsonb、status varchar(16)、result_ref jsonb?、error jsonb?。

约束：unique(oid, actor_id, command_type, command_id)。status 为 RECEIVED / APPLIED / REJECTED；同键不同请求指纹返回冲突。

### 2.4 chatbi_agent_run

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| run_id | varchar(64) | 外部 ID |
| oid / owner_user_id | bigint | 工作空间与所有者 |
| conversation_id / message_id | bigint FK | 会话和入口消息 |
| parent_run_id | bigint FK? | 追问、报告或重跑来源 |
| run_type | varchar(16) | INTERACTIVE / REPORT / MONITOR |
| status | varchar(32) | C1 RunStatus |
| completion_level | varchar(16)? | FINISHED 时 COMPLETE / PARTIAL |
| dataset_id / datasource_id | bigint | 单数据集与单数据源 |
| semantic_view_id | varchar(128) | 固定语义视图 |
| contract_version_id | bigint? | 准备成功后固定 |
| permission_snapshot_id / runtime_snapshot_id / data_snapshot_id | bigint FK? | 准备阶段写入 |
| current_question_context_id | bigint FK? | 当前需求修订 |
| active_confirmation_id | bigint FK? | 单个 ACTIVE 确认 |
| wait_parent_run_id | bigint FK? | 等待父结果 |
| scheduling_reason | varchar(64)? | CAPACITY / USER_CAPACITY / PARENT_RESULT |
| reference_at | timestamptz | 时间解析基准 |
| timezone | varchar(64) | IANA 时区 |
| priority | smallint | 交互高于监控 |
| budget_reserved / budget_consumed | jsonb | 分项预算，使用固定 Schema |
| last_event_seq | bigint | 初值 0 |
| error_code / error_summary | varchar(128)? / text? | 安全错误 |
| started_at / finished_at / cancel_requested_at | timestamptz? | 生命周期时间 |
| lock_version | bigint | 状态更新版本 |

约束：unique(run_id)；completion_level 仅在 FINISHED 非空；终态 finished_at 非空；进入 Agent PLANNING 前四类快照及 QuestionContext 必须存在。约束依赖关联表时由服务事务校验，并配置数据库触发器阻止绕过写入。

索引：(oid, owner_user_id, created_at desc)、(conversation_id, created_at)、(status, priority desc, created_at) where status in ('CREATED','RUNNING','CANCELLING')、(wait_parent_run_id) where wait_parent_run_id is not null。

## 3. Agent、计划和子任务

### 3.1 chatbi_agent_instance

字段：agent_id varchar(64)、run_id bigint FK、role varchar(32)、parent_agent_id bigint FK?、subtask_id bigint?、phase varchar(32)、context_revision bigint、decision_generation bigint、lease_owner varchar(128)?、lease_expires_at timestamptz?、heartbeat_at timestamptz?、next_wake_at timestamptz?、lock_version bigint。

约束：unique(agent_id)；unique(run_id) where role='MAIN'；MAIN 的 parent_agent_id/subtask_id 为空，子 Agent 两字段非空；子 Agent 的父实例必须为同 Run MAIN。role 与 phase 枚举使用 C1 定义。

### 3.2 chatbi_plan

字段：plan_id varchar(64)、run_id bigint FK、agent_id bigint FK、goal text、active_version integer、lock_version bigint。

约束：unique(plan_id)、unique(agent_id)。计划归属 Agent，不能用 unique(run_id) 限制同 Run 的多个 LocalPlan。

### 3.3 chatbi_plan_version

字段：plan_id bigint FK、version integer、base_version integer?、reason text、structure jsonb、content_fingerprint varchar(80)、source_decision_id bigint?。

约束：unique(plan_id, version)、version>=1、base_version<version。structure 保存该版本完整 PlanStepDefinition；写入后不可更新。active_version 是 Plan 上唯一当前指针，不在多个版本行分别维护 ACTIVE 状态。

### 3.4 chatbi_plan_step

字段：plan_id bigint FK、step_id varchar(64)、introduced_version integer、status varchar(32)、started_at timestamptz?、finished_at timestamptz?、lock_version bigint。

约束：unique(plan_id, step_id)。description、acceptance_criteria、depends_on 属于 PlanVersion.structure，不重复保存在可变步骤表。调度从当前版本结构读取依赖；结果关联稳定步骤行。

### 3.5 chatbi_step_result

字段：step_result_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、status varchar(16)、execution_refs jsonb、evidence_ids jsonb、covered_requirement_ids jsonb、completion_summary text、error jsonb?、skip_reason text?、content_fingerprint varchar(80)。

约束：unique(step_result_id)、unique(step_pk)；status 只能为步骤终态；不可更新或替换。跨 Plan 的输入使用外部 StepResult ID，不用可能重名的 step_id。

### 3.6 chatbi_subagent_task

字段：subtask_id varchar(64)、run_id bigint FK、parent_agent_id bigint FK、parent_step_pk bigint FK、agent_id bigint FK、source_decision_id bigint FK、task_name varchar(256)、instruction text、input_step_result_ids jsonb、status varchar(32)、result_id bigint?、finished_at timestamptz?、lock_version bigint。

约束：unique(subtask_id)、unique(agent_id)、unique(source_decision_id, agent_id)。父实例必须 MAIN；agent_id 必须属于当前 Run 和允许子角色。任务与子 Agent 创建事务使用可延迟外键处理双向关联，提交前两侧引用完整。

### 3.7 chatbi_subagent_result

字段：subtask_result_id varchar(64)、subtask_id bigint FK、status varchar(16)、completion_level varchar(16)、summary text、step_result_ids jsonb、result_refs jsonb、evidence_ids jsonb、limitations jsonb、content_fingerprint varchar(80)。

约束：unique(subtask_result_id)、unique(subtask_id)。结果不可变，子任务终态与父步骤 Observation 同事务生成。

## 4. 决策、等待与调度

### 4.1 chatbi_agent_decision

字段：decision_id varchar(64)、run_id bigint FK、agent_id bigint FK、generation bigint、context_revision bigint、plan_version integer?、phase varchar(32)、decision_type varchar(32)?、status varchar(16)、input_fingerprint varchar(80)、model_invocation_id bigint?、function_calls jsonb?、decision_summary text?、commentary text?、validation_error jsonb?、committed_at timestamptz?。

status 为 CLAIMED / APPLIED / REJECTED / STALE / FAILED。约束：unique(decision_id)、unique(agent_id, generation)。CLAIMED 时允许尚无模型输出；APPLIED 必须已通过上下文和租约校验。STALE 不创建 ToolCall。

### 4.2 chatbi_agent_trigger

字段：agent_id bigint FK、cause_type varchar(64)、cause_id varchar(128)、status varchar(16)、available_at timestamptz、claimed_generation bigint?、consumed_at timestamptz?。

约束：unique(agent_id, cause_type, cause_id)；status 为 PENDING / CLAIMED / CONSUMED。索引：(available_at, id) where status='PENDING'。生成触发器与原因事件同事务；消费失败可重领。

### 4.3 chatbi_wait_subscription

字段：wait_id varchar(64)、agent_id bigint FK、step_pk bigint FK?、policy varchar(16)、targets jsonb、required_ids jsonb、status varchar(16)、wake_cause_ref jsonb?、resolved_at timestamptz?。

policy 为 ALL / ANY / REQUIRED；targets 非空；REQUIRED 的 required_ids 非空且为 targets 子集。status 为 WAITING / RESOLVED / CANCELLED。unique(agent_id) where status='WAITING'。目标终态包含成功、失败和取消。

### 4.4 配额

chatbi_quota_reservation 字段：reservation_id varchar(64)、scope_type varchar(32)、scope_id varchar(128)、resource_type varchar(32)、owner_type varchar(32)、owner_id varchar(64)、amount bigint、status varchar(16)、expires_at timestamptz。

unique(scope_type, scope_id, resource_type, owner_type, owner_id)。状态 RESERVED / CONSUMED / RELEASED。并发和预算预留在锁定对应配额计数行后完成，不能仅依赖应用进程计数。子任务共享 Run 预算。

## 5. 工具、查询和结果

### 5.1 chatbi_tool_call

字段：tool_call_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、decision_id bigint FK、ordinal integer、tool_name varchar(128)、tool_version varchar(64)、arguments jsonb、argument_fingerprint varchar(80)、idempotency_key varchar(256)、status varchar(32)、generation bigint、next_attempt_at timestamptz?、result_id bigint?、lock_version bigint。

约束：unique(tool_call_id)、unique(decision_id, ordinal)、unique(run_id, idempotency_key)。status 使用 C1.ToolCallStatus。索引：(status, next_attempt_at) where status='PENDING'。

### 5.2 chatbi_tool_attempt

字段：attempt_id varchar(64)、tool_call_id bigint FK、generation bigint、status varchar(16)、worker_id varchar(128)、lease_expires_at timestamptz、heartbeat_at timestamptz、started_at timestamptz、finished_at timestamptz?、error jsonb?、diagnostic_artifact_id bigint?。

status 为 CLAIMED / RUNNING / SUCCEEDED / FAILED / LOST / CANCELLED / STALE。unique(tool_call_id, generation)；unique(tool_call_id) where status in ('CLAIMED','RUNNING')。提交必须匹配调用当前 generation，不仅检查输出是否为空。

### 5.3 chatbi_tool_result 与 chatbi_observation

ToolResult 字段：tool_result_id varchar(64)、tool_call_id bigint FK、status varchar(16)、output_type varchar(32)、output jsonb、evidence_ids jsonb、limitations jsonb、usage jsonb、error jsonb?、content_fingerprint varchar(80)。unique(tool_call_id)，不可变。

Observation 字段：observation_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、source_type varchar(32)、source_id varchar(64)、tool_result_id bigint FK?、subtask_result_id bigint FK?、control_payload jsonb?。unique(agent_id, source_type, source_id)。来源字段按判别类型互斥。

### 5.4 chatbi_question_context 与 chatbi_semantic_binding

QuestionContext 字段：question_context_id varchar(64)、run_id bigint FK、revision integer、content jsonb、fingerprint varchar(80)、source_command_id bigint FK?。unique(run_id, revision)，不可变。

chatbi_requirement_acceptance 字段：run_id bigint FK、question_context_id bigint FK、requirement_id varchar(64)、content jsonb、fingerprint varchar(80)。unique(question_context_id, requirement_id)，内容按 C2.RequirementAcceptance 保存且不可变。

SemanticBinding 字段：binding_id varchar(64)、run_id bigint FK、tool_call_id bigint FK、question_context_id bigint FK、contract_version_id bigint、status varchar(16)、input jsonb、bound_content jsonb?、ambiguities jsonb、fingerprint varchar(80)。status 为 VALID / AMBIGUOUS / UNSUPPORTED。

### 5.5 chatbi_semantic_query

字段：query_id varchar(64)、run_id bigint FK、tool_call_id bigint FK、binding_id bigint FK、request jsonb、compiled_query_id varchar(64)、logical_plan_artifact_id bigint FK、sql_artifact_id bigint FK、parameter_artifact_id bigint FK、result_schema jsonb、lineage jsonb、cost_estimate jsonb、permission_fingerprint varchar(80)、fingerprint varchar(80)。不可变，unique(query_id)、unique(compiled_query_id)。

### 5.6 chatbi_query_execution

字段：query_execution_id varchar(64)、tool_attempt_id bigint FK、semantic_query_id bigint FK、data_snapshot_id bigint FK、snapshot_generation bigint、ordinal integer、status varchar(16)、started_at timestamptz?、finished_at timestamptz?、row_count bigint?、result_id bigint?、error jsonb?。

status 为 PENDING / RUNNING / SUCCEEDED / FAILED / CANCELLED / STALE。unique(tool_attempt_id, ordinal)。一个 ToolCall 内可有两期查询；不额外创建业务计划节点。

### 5.7 chatbi_result_set

字段：result_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、tool_call_id bigint FK、query_execution_id bigint FK?、kind varchar(16)、schema jsonb、row_count bigint、completeness varchar(32)、limit_reason varchar(128)?、artifact_id bigint FK、contract_version_id bigint、permission_snapshot_id bigint FK、data_snapshot_id bigint FK、question_context_id bigint FK、lineage jsonb、content_fingerprint varchar(80)。

kind 为 QUERY / COMPUTED，completeness 使用 C3 枚举。正式结果不可变。查询结果可作为工具内部输入，但只有通过完整工具提交事务后才对外 AVAILABLE。

### 5.8 chatbi_analysis_result 与 chatbi_evidence

AnalysisResult 字段：analysis_result_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、tool_call_id bigint FK、analysis_type varchar(64)、algorithm_version varchar(64)、analysis_contract_ref varchar(128)、input_refs jsonb、content jsonb、limitations jsonb、fingerprint varchar(80)。

Evidence 字段：evidence_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、type varchar(32)、source_refs jsonb、facts jsonb、limitations jsonb、contract_version_id bigint、permission_snapshot_id bigint FK、data_snapshot_id bigint FK?、fingerprint varchar(80)。仅 SEMANTIC_FACT 允许无数据快照。来源必须是当前 Run 正式结果或固定发布语义事实。

### 5.9 下钻

chatbi_drilldown_session：session_id varchar(64)、run_id bigint FK、agent_id bigint FK、owner_step_pk bigint FK、root_metric_key varchar(128)、baseline_ref jsonb、status varchar(32)、version bigint、automatic_depth_limit integer、confirmed_depth_limit integer、hard_depth_limit integer、visited_keys jsonb、frontier jsonb、budget_usage jsonb。

chatbi_drilldown_candidate_set：candidate_set_id varchar(64)、session_id bigint FK、session_version bigint、candidates jsonb、fingerprint varchar(80)，不可变。

chatbi_drilldown_result：drilldown_result_id varchar(64)、candidate_set_id bigint FK、candidate_id varchar(64)、tool_call_id bigint FK、content jsonb、merged_at timestamptz?。unique(candidate_set_id, candidate_id)。会话合并按 session_version 条件更新一次；单个并行工具不直接更新共享会话。

## 6. 快照与模型

### 6.1 快照表

| 表 | 专有字段 |
| --- | --- |
| chatbi_runtime_snapshot | runtime_snapshot_id varchar(64)、config_version varchar(64)、content jsonb、fingerprint varchar(80) |
| chatbi_permission_snapshot | permission_snapshot_id varchar(64)、oid bigint、owner_user_id bigint、dataset_id bigint、semantic_view_id varchar(128)、policy_version varchar(64)、permission_fingerprint varchar(80)、allowed_asset_keys jsonb、field_policy_artifact_id bigint、row_policy_artifact_id bigint、masking_policy_ref varchar(128) |
| chatbi_data_snapshot | data_snapshot_id varchar(64)、datasource_id bigint、mode varchar(32)、status varchar(16)、owner_id varchar(128)?、generation bigint、source_token_artifact_id bigint?、opened_at timestamptz?、expires_at timestamptz?、consistency_level varchar(16)、capability_version varchar(64)、lock_version bigint |
| chatbi_model_route_snapshot | route_snapshot_id varchar(64)、task_routes jsonb、fingerprint varchar(80) |

数据快照 mode/status 遵循 C2，不保存不可验证的一致性声明。Token 只存加密制品。Run 与快照一对一归属通过 Run 外键唯一索引约束；运行配置和模型路由允许内容寻址复用，但不能修改已有内容。

### 6.2 chatbi_model_invocation

字段：invocation_id varchar(64)、run_id bigint FK、agent_id bigint FK?、decision_id bigint FK?、task_name varchar(64)、route_snapshot_id bigint FK、prompt_ref varchar(128)、context_revision bigint、input_fingerprint varchar(80)、status varchar(16)、provider_request_id varchar(256)?、output_artifact_id bigint?、usage jsonb、error jsonb?、started_at timestamptz、finished_at timestamptz?。

chatbi_model_attempt 保存 invocation_id bigint FK、attempt_no integer、status varchar(16)、provider_request_id varchar(256)?、usage jsonb、error jsonb?、started_at、finished_at?。unique(invocation_id, attempt_no)。用量未知使用显式状态，不能记作零。

## 7. 确认与事件

### 7.1 chatbi_confirmation

字段：confirmation_id varchar(64)、run_id bigint FK、agent_id bigint FK、step_pk bigint FK、type varchar(32)、question text、options jsonb、resume_strategy varchar(32)、frozen_tool_call_id bigint FK?、argument_fingerprint varchar(80)?、status varchar(16)、response_command_id bigint FK?、response jsonb?、resolved_at timestamptz?。

status 为 PENDING / ACTIVE / ACCEPTED / REJECTED / CANCELLED。unique(run_id) where status='ACTIVE'；排队索引(run_id, status, created_at, id)。RESUME_TOOL_CALL 必须有冻结调用和参数指纹。

### 7.2 chatbi_run_event

字段：run_id bigint FK、event_seq bigint、event_type varchar(64)、schema_version varchar(16)、payload jsonb、occurred_at timestamptz、trace_id varchar(64)。unique(run_id, event_seq)。事件不更新；安全投影遵循 C4，不改变持久化序号。

生成事件时锁定 Run 行，分配 last_event_seq+1，同时写事件和更新序号。批量事件一次分配连续区间。不能使用进程内计数或 Redis 作为唯一序号来源。

## 8. 内容、制品与产品对象

| 表 | 专有字段与约束 |
| --- | --- |
| chatbi_answer | answer_id varchar(64)、run_id FK、version int、status varchar(16)、completion_level varchar(16)、evidence_ids jsonb、limitations jsonb；unique(run_id,version) |
| chatbi_answer_block | answer_id FK、ordinal int、title text、content_template text、numeric_bindings jsonb、rendered_markdown text、evidence_ids jsonb；unique(answer_id,ordinal) |
| chatbi_chart | chart_id varchar(64)、run_id FK、result_id FK、version int、spec jsonb、fingerprint varchar(80) |
| chatbi_report | report_id varchar(64)、run_id FK、version int、title text、blocks jsonb、source_refs jsonb、limitations jsonb、fingerprint varchar(80) |
| chatbi_artifact | artifact_id varchar(64)、oid bigint、classification varchar(32)、storage_key text、content_hash varchar(80)、size_bytes bigint、schema_version varchar(16)、status varchar(16)、expires_at timestamptz? |
| chatbi_feedback | feedback_id varchar(64)、run_id FK、user_id bigint、target_ref jsonb?、type varchar(16)、comment text? |
| chatbi_user_resource_state | oid bigint、user_id bigint、resource_type varchar(32)、resource_id varchar(64)、deleted_at timestamptz；unique(user_id,resource_type,resource_id) |
| chatbi_monitor_task | monitor_id varchar(64)、owner_user_id bigint、conversation_id FK、question text、schedule jsonb、status varchar(16)、next_run_at timestamptz、lock_version bigint |
| chatbi_monitor_execution | monitor_id FK、scheduled_at timestamptz、run_id FK；unique(monitor_id,scheduled_at) |
| chatbi_notification | notification_id varchar(64)、owner_user_id bigint、run_id FK、type varchar(32)、title text、read_at timestamptz? |
| chatbi_audit_record | audit_id varchar(64)、oid bigint、actor_id varchar(64)、action varchar(64)、target_ref jsonb、run_id bigint?、decision_id bigint?、tool_call_id bigint?、outcome varchar(16)、metadata jsonb、trace_id varchar(64)、occurred_at timestamptz |

AnswerBlock 对外 block_id 使用外部 ID，并与 answer_id、ordinal 建唯一约束。所有图表、报告及回答正文校验后不可变；修改生成新版本。监控问题走同一 Run / Agent 流程，不维护第二套执行引擎。

## 9. 事务边界

### 9.1 Run 创建和准备

创建事务：命令幂等记录、消息、CREATED Run、MAIN Agent、准备触发器、事件和审计。准备在事务外读取外部服务，完成后短事务写入快照与 QuestionContext 并推进阶段。不得在数据库事务内等待模型或数据源网络调用。

### 9.2 决策提交

锁定顺序统一为 Run → Agent → Plan → Step → ToolCall / Subtask → Artifact 引用。检查 generation、租约、context_revision、plan_version 和 Run 状态后，保存决策、计划变更或 ToolCall、配额预留、事件、审计和触发器。任何一项失败整体回滚。

### 9.3 工具完成

先将结果正文写入不可变 STAGED 制品，再执行提交事务：校验当前 Attempt 代次及租约 → 制品元数据转 AVAILABLE → ResultSet / AnalysisResult / Evidence → ToolResult → Observation → 调用终态 → 事件、审计及唤醒。失败的 STAGED 制品由清理任务回收。

### 9.4 步骤和子任务完成

完成步骤与唯一 StepResult 同事务。完成子任务与 SubAgentTaskResult、父步骤 Observation、等待订阅更新及触发器同事务。ANY 唤醒不提前结束其余子任务。

### 9.5 确认与取消

确认响应锁定 Run 和 ACTIVE 确认，校验命令指纹，保存响应、上下文修订或冻结调用恢复、下一确认、事件和唤醒。取消先将 Run 转 CANCELLING 并失效决策版本，外部取消在提交后执行；完成事件与结果事务以 Run 行锁确定先后顺序。

### 9.6 回答提交

整份候选校验完成后，事务保存 Answer 和所有块、更新 Run 为 FINISHED、写入内容事件和 RUN_FINISHED。提交前复核权限与上下文版本。事务失败不得向 SSE 发布成功内容。

## 10. 恢复、清理与容量

调度器通过 FOR UPDATE SKIP LOCKED 领取 PENDING 触发器和工具调用；租约过期后增加 generation 并重领。元数据库连接释放后才调用模型或数据源。恢复扫描同时检查快照有效性，不能只根据 ToolAttempt 状态重试。

制品采用先写内容、后提交引用；清理扫描无正式引用且超过保护期的 STAGED 对象。AVAILABLE 制品只有在全部业务引用达到保留期后才可删除。默认保留期与 C6 一致。

首期使用普通表和必要索引，容量基准为 50 在线用户、100 活动 Run。事件和调用记录达到千万级后按 created_at 月分区；分区迁移必须保留业务唯一键或独立去重表，不能因 PostgreSQL 分区唯一约束限制丢失幂等保证。

## 11. 数据库验收

必须验证：一个 Run 多 LocalPlan；同 Agent 单计划；同一步骤单结果；旧决策和旧 Attempt 拒绝提交；并行确认单 ACTIVE；事件序号无重复；计划版本冲突整体回滚；外部调用期间不持有行锁；STAGED 清理不删除正式引用；取消与完成竞态；监控时刻重复调度只创建一个 Run。

## 12. 语义层公共字段

### 12.1 编辑态资产标准字段

除纯关系明细表外，语义资产统一包含：

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 主键 |
| `oid` | bigint | 否 | 工作空间 |
| `dataset_id` | bigint | 否 | 语义项目/数据集 |
| `domain_id` | bigint | 是 | 业务域 |
| `biz_name` | varchar(128) | 否 | 展示名 |
| `name` | varchar(128) | 否 | 稳定业务编码 |
| `description` | text | 是 | 中文说明 |
| `contract_status` | varchar(16) | 否 | `DRAFT / IN_REVIEW / APPROVED / PUBLISHED` |
| `status` | varchar(16) | 否 | ACTIVE / DISABLED / DEPRECATED |
| `version` | bigint | 否 | 资产版本，只增不减 |
| `ext` | jsonb | 否 | 有界扩展属性 |
| `created_by` | bigint | 是 | 创建人 |
| `updated_by` | bigint | 是 | 修改人 |
| `created_at` | timestamptz | 否 | 创建时间 |
| `updated_at` | timestamptz | 否 | 更新时间 |

同类资产默认唯一约束：`unique(oid, dataset_id, name)`。

### 12.2 资产版本规则

- 编辑保存采用乐观锁，成功修改后 `version = version + 1`。
- 提交审核保存提交内容指纹和每个资产版本。
- 审核批准绑定提交指纹。
- 发布生成新的不可变 `headless_semantic_contract_version`。
- 回滚以历史发布内容创建新草稿，资产和合约版本均只增不减。
- 发布版本不依赖编辑态行的当前内容解释历史结果。

## 13. 语义项目、模型和维度表

### 13.1 语义概念到数据库表映射

A 的语义概念与目标数据库表映射如下：

| A 文档概念 | 目标表 | 说明 |
| --- | --- | --- |
| `semantic_project` | `headless_dataset` | 可独立发布的语义项目，也是前端选择的数据集 |
| `semantic_domain` | `headless_domain` | 业务域 |
| `semantic_calendar` | `headless_calendar` | 独立时间口径 |
| `data_source` | `core_datasource` | 由 datasource 模块拥有 |
| `physical_table` | `core_table` | 由 datasource 模块拥有 |
| `physical_column` | `core_field` | 由 datasource 模块拥有 |
| `semantic_model` | `headless_model` | 语义模型 |
| `model_field` | `headless_model_field` | 模型字段 |
| `model_measure` | `headless_model_measure` | 模型度量 |
| `dimension` | `headless_logical_dimension` | 逻辑维度 |

语义模块只保存 `core_*` 对象的逻辑引用和同步指纹，不复制数据源连接、表、字段事实。

### 13.2 headless_dataset

作为 A 的 semantic_project 的物理实现，字段如下：

- 标准资产字段中的 `id`、`oid`、`biz_name`、`name`、`description`、`status`、版本和审计字段；
- `project_key varchar(128) not null`：项目稳定编码；
- `datasource_id bigint not null`：唯一业务数据源；
- `default_dialect varchar(32) not null`：默认 SQL 方言；
- `default_calendar_id bigint null`：默认日历；
- `active_contract_version_id bigint null`：当前发布版本；
- `repo_url text null`：语义定义仓库地址；
- `owner varchar(128) null`：项目负责人；
- `settings jsonb not null`：时区、默认语言和语义检索配置。

约束：

- `unique(oid, project_key)`；
- 一个数据集只绑定一个数据源；
- `active_contract_version_id` 必须属于当前数据集且状态为 ACTIVE。

### 13.3 headless_domain

保存业务域：标准资产字段 + `parent_domain_id bigint null`、`domain_type varchar(32)`、`boundary_desc text`、`default_entity_id bigint null`、`owner_team varchar(128)`、`path varchar(512)`、`depth integer`。

约束：

- `unique(oid, dataset_id, name)`；
- `unique(oid, dataset_id, parent_domain_id, name)`；
- 业务域层级无环。

### 13.4 headless_calendar

| 专有字段 | 类型 | 说明 |
| --- | --- | --- |
| `calendar_type` | varchar(32) | `NATURAL / FISCAL / BUSINESS / RETAIL` |
| `timezone` | varchar(64) | 时区 |
| `week_start_day` | smallint | 1～7 |
| `fiscal_year_start_month` | smallint | 1～12 |
| `calendar_table_ref` | varchar(128) nullable | 日历表逻辑引用 |
| `period_rules` | jsonb | 自定义周期规则 |

使用标准资产字段。`check(week_start_day between 1 and 7)`，`check(fiscal_year_start_month between 1 and 12)`。

### 13.5 headless_term

企业术语是语义资产，不是 Agent 提示词。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `dataset_id` | bigint | 语义项目 |
| `domain_id` | bigint | 业务域 |
| `term_key` | varchar(128) | 稳定编码 |
| `biz_name` | varchar(128) | 中文术语 |
| `aliases` | jsonb | 同义词和历史叫法 |
| `description` | text | 业务定义 |
| `related_asset_refs` | jsonb | 指标、维度、实体等引用 |
| `usage_examples` | jsonb | 经过治理的使用示例 |
| `contract_status` | varchar(16) | 编辑发布状态 |
| `version` | bigint | 版本 |
| `status` | varchar(16) | ACTIVE / DISABLED / DEPRECATED |
| `created_by / updated_by` | bigint | 操作者 |
| `created_at / updated_at` | timestamptz | 时间 |

约束：

- `unique(oid, dataset_id, term_key)`；
- 发布态术语进入 SearchDocument；
- 术语不能覆盖指标公式、维度能力和权限规则。

### 13.6 物理基础引用

`core_datasource`、`core_table`、`core_field` 继续由 datasource 模块维护。语义层使用以下稳定字段：

| 表 | 必需引用字段 |
| --- | --- |
| `core_datasource` | `id`、`oid`、数据源类型、数据库类型、默认库和 Schema、元数据同步状态、最近同步时间、状态 |
| `core_table` | `id`、`datasource_id`、库名、Schema、表名、对象类型、行数估算、DDL 指纹、最近同步时间、状态 |
| `core_field` | `id`、`table_id`、字段名、原始类型、归一类型、顺序、可空性、主键/分区/时间标记、统计摘要、敏感分类、最近同步时间、状态 |

语义表使用 `datasource_id`、`physical_table_ref` 和 `physical_column_ref` 引用这些对象。连接密钥仍只存在于 datasource 受限配置中。

### 13.7 headless_business_entity

专有字段：

- `key_type varchar(64) not null`；
- `value_domain_key varchar(128) null`；
- `default_label_dimension_id bigint null`。

约束：`unique(oid, dataset_id, name)`。

### 13.8 headless_model

专有字段：

- `model_type varchar(32)`：`TABLE / VIEW / SQL`；
- `physical_table_ref varchar(128) null`；
- `source_sql_ast jsonb null`；
- `source_sql_text text null`；
- `primary_entity_id bigint null`；
- `default_time_field_id bigint null`；
- `grain_entity_refs jsonb not null`；
- `model_capabilities jsonb not null`。

约束：

- TABLE/VIEW 必须有 `physical_table_ref`；
- SQL 模型必须有通过只读校验的 `source_sql_ast`；
- 模型粒度必须可由实体绑定验证。

### 13.9 headless_model_field

| 专有字段 | 类型 | 说明 |
| --- | --- | --- |
| `model_id` | bigint FK | 所属模型 |
| `physical_column_ref` | varchar(128) nullable | 物理列引用 |
| `field_role` | varchar(32) | `DIMENSION / ENTITY_KEY / TIME / DETAIL` |
| `value_type` | varchar(32) | 数据类型 |
| `semantic_type` | varchar(64) | 业务语义类型 |
| `expr_ast` | jsonb nullable | 派生表达式 |
| `expr_text` | text nullable | 展示表达式 |
| `nullable` | boolean | 是否可空 |
| `sensitivity` | varchar(32) | 敏感级别 |
| `can_filter` | boolean | 可筛选 |
| `can_group` | boolean | 可分组 |
| `can_expose_as_detail` | boolean | 可发布为明细字段 |
| `format_policy` | jsonb | 展示格式 |

约束：`unique(model_id, name)`；明细发布要求 `can_expose_as_detail=true`。

### 13.10 headless_model_measure

| 专有字段 | 类型 | 说明 |
| --- | --- | --- |
| `model_id` | bigint FK | 所属模型 |
| `aggregation` | varchar(32) | `SUM / COUNT / COUNT_DISTINCT / AVG / MIN / MAX / CUSTOM` |
| `expr_ast` | jsonb | 度量表达式 |
| `expr_text` | text | 展示表达式 |
| `value_type` | varchar(32) | 结果类型 |
| `unit` | varchar(32) nullable | 单位 |
| `additivity` | varchar(32) | 可加性 |
| `format_policy` | jsonb | 格式策略 |

约束：`unique(model_id, name)`。

### 13.11 headless_model_entity_binding

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `model_id` | bigint FK | 模型 |
| `entity_id` | bigint FK | 业务实体 |
| `field_id` | bigint FK | 实体键字段 |
| `binding_role` | varchar(32) | `PRIMARY / FOREIGN / NATURAL_KEY` |
| `binding_quality` | varchar(16) | `EXPLICIT / INFERRED / REVIEW_REQUIRED` |
| `version` | bigint | 版本 |
| `created_at` | timestamptz | 创建时间 |
| `updated_at` | timestamptz | 更新时间 |

约束：`unique(model_id, entity_id, binding_role)`。

### 13.12 headless_model_relation

专有字段：

- `left_model_id bigint`、`right_model_id bigint`；
- `relationship_type varchar(32)`；
- `cardinality varchar(16)`：`ONE_TO_ONE / ONE_TO_MANY / MANY_TO_ONE / MANY_TO_MANY`；
- `join_type varchar(16)`；
- `condition_ast jsonb`；
- `entity_id bigint null`；
- `aggregation_safety varchar(32)`；
- `bidirectional boolean`；
- `priority integer`。

约束：

- `unique(oid, left_model_id, right_model_id, name)`；
- 左右模型不能相同；
- 关系条件只能引用两个模型已发布字段；
- MANY_TO_MANY 必须声明聚合风险处理。

### 13.13 headless_logical_dimension

专有字段：

- `entity_id bigint null`；
- `semantic_type varchar(64)`；
- `value_type varchar(64)`；
- `value_domain_key varchar(128) null`；
- `default_value_set_id bigint null`；
- `format_policy jsonb`；
- `filter_operators jsonb`；
- `high_cardinality boolean`。

约束：`unique(oid, dataset_id, name)`。

### 13.14 headless_dimension_binding

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `logical_dimension_id` | bigint FK | 逻辑维度 |
| `model_id` | bigint FK | 模型 |
| `model_field_id` | bigint FK | 模型字段 |
| `binding_role` | varchar(32) | `PRIMARY / ALTERNATE` |
| `binding_quality` | varchar(16) | 绑定质量 |
| `value_mapping_required` | boolean | 是否需要标准值映射 |
| `version` | bigint | 版本 |
| `created_at` | timestamptz | 创建时间 |
| `updated_at` | timestamptz | 更新时间 |

约束：`unique(logical_dimension_id, model_id, model_field_id)`。

### 13.15 维度值和层级表

`headless_dimension_value_set` 专有字段：

- `logical_dimension_id bigint`；
- `value_set_type varchar(32)`：`ENUM / RANGE / EXTERNAL`；
- `normalization_policy jsonb`；
- `is_default boolean`。

`headless_dimension_value` 专有字段：

- `value_set_id bigint`；
- `value_key varchar(256)`；
- `display_name varchar(256)`；
- `aliases jsonb`；
- `sort_order integer`；
- `parent_value_id bigint null`；
- `active boolean`。

约束：`unique(value_set_id, value_key)`。

`headless_dimension_mapping` 字段：

- `id bigint identity PK`；
- `oid bigint`；
- `dimension_binding_id bigint`；
- `raw_value varchar(512)`；
- `standard_value_id bigint`；
- `match_type varchar(16)`；
- `version bigint`；
- 审计时间和操作者。

约束：`unique(dimension_binding_id, raw_value)`。

`headless_dimension_hierarchy` 使用标准资产字段，并增加 `hierarchy_type`、`maximum_depth`、`recommended_depth`、`review_policy`。

`headless_dimension_hierarchy_level` 字段：

- `id bigint identity PK`；
- `hierarchy_id bigint FK`；
- `logical_dimension_id bigint FK`；
- `level_order integer`；
- `requires_review boolean`；
- `rollup_expr_ast jsonb null`。

约束：

- `unique(hierarchy_id, level_order)`；
- `unique(hierarchy_id, logical_dimension_id)`；
- `maximum_depth` 不能超过实际层级数。

## 14. 指标与分析语义表

### 14.1 headless_metric

指标专有字段：

| 专有字段 | 类型 | 说明 |
| --- | --- | --- |
| `metric_type` | varchar(32) | `SIMPLE / DERIVED / RATIO / CUMULATIVE` |
| `expr_ast` | jsonb | 指标公式 |
| `expr_text` | text | 展示公式 |
| `value_type` | varchar(32) | 结果类型 |
| `unit` | varchar(32) nullable | 单位 |
| `format_policy` | jsonb | 精度、币种和展示规则 |
| `default_time_grain` | varchar(16) nullable | 默认时间粒度 |
| `requires_time_filter` | boolean | 是否必须有时间条件 |
| `applicable_scenarios` | jsonb | 适用场景 |
| `inapplicable_scenarios` | jsonb | 不适用场景 |
| `confusable_metric_refs` | jsonb | 易混淆指标 |
| `driver_analysis_enabled` | boolean | 可否作为归因目标 |
| `anomaly_detection_enabled` | boolean | 可否异常分析 |
| `analysis_policy` | jsonb | C3 异常 profile、缺失值策略和分析规则 |
| `change_decomposition_method` | varchar(64) nullable | 发布分解方法 |
| `absolute_tolerance` | numeric(38,10) | 使用指标单位的绝对容差 |
| `relative_tolerance` | numeric(20,10) | 无量纲相对容差 |
| `version` | bigint | 资产版本 |

约束：

- `unique(oid, dataset_id, name)`；
- 指标公式只能引用发布范围可用输入；
- 可归因指标至少有一个可分析输入或依赖边。

### 14.2 headless_metric_input_binding

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `metric_id` | bigint FK | 目标指标 |
| `input_key` | varchar(128) | 公式输入名 |
| `input_type` | varchar(32) | `MEASURE / FIELD / METRIC / CONSTANT / PARAMETER` |
| `measure_id` | bigint nullable | 度量输入 |
| `model_field_id` | bigint nullable | 字段输入 |
| `input_metric_id` | bigint nullable | 指标输入 |
| `constant_value` | jsonb nullable | 常量 |
| `parameter_spec` | jsonb nullable | 参数 |
| `formula_role` | varchar(32) | `FACTOR / NUMERATOR / DENOMINATOR / ADDEND / SUBTRAHEND / BASE` |
| `can_analyze_as_driver` | boolean | 可继续分析驱动 |
| `driver_priority` | integer | 驱动优先级 |
| `expected_direction` | varchar(16) | 预期方向 |
| `version` | bigint | 版本 |
| `created_at` | timestamptz | 创建时间 |
| `updated_at` | timestamptz | 更新时间 |

约束：

- `unique(metric_id, input_key)`；
- 五类输入引用必须且只能设置一种；
- `input_metric_id <> metric_id`；
- 发布校验指标依赖无环。

### 14.3 headless_metric_filter

字段：

- `id bigint identity PK`；
- `oid bigint`；
- `metric_id bigint FK`；
- `filter_order integer`；
- `logical_dimension_id bigint`；
- `operator varchar(32)`；
- `value_spec jsonb`；
- `condition_ast jsonb`；
- `version bigint`；
- 审计字段。

约束：`unique(metric_id, filter_order)`；操作符必须属于维度能力。

### 14.4 headless_metric_dependency

该表是发布编译派生索引，不是人工编辑事实源。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `contract_version_id` | bigint FK | 发布版本 |
| `target_metric_id` | bigint | 目标指标 |
| `driver_metric_id` | bigint | 驱动指标 |
| `path_depth` | integer | 依赖深度 |
| `dependency_path` | jsonb | 路径 |
| `formula_role` | varchar(32) | 公式角色 |
| `can_analyze_as_driver` | boolean | 可分析 |
| `expected_direction` | varchar(16) | 预期方向 |
| `content_fingerprint` | varchar(80) | 指纹 |
| `created_at` | timestamptz | 创建时间 |

约束：

- `unique(contract_version_id, target_metric_id, driver_metric_id, dependency_path)`；
- 发布版本内无环；
- 只能由发布编译器写入。

### 14.5 headless_metric_dimension_capability

标准资产字段之外的专有字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| metric_id / logical_dimension_id | bigint FK | 指标与维度 |
| can_group_by / can_filter / can_order_by / can_drill_down / can_contribute | boolean | 对应 A 的操作能力 |
| binding_strategy | varchar(32) | DIRECT / ENTITY_JOIN / BRIDGE / PRE_AGG / DENORMALIZED |
| relation_path | jsonb | 冻结模型关系路径 |
| target_model_id | bigint FK | 目标模型 |
| dimension_binding_id | bigint FK | 对应 A.dimension_modelField_binding |
| aggregation_safety | varchar(32) | SAFE / REQUIRES_PRE_AGG / UNSAFE |
| pre_aggregation_grain | jsonb | 预聚合粒度 |
| supported_time_grains | jsonb | 允许时间粒度 |
| time_alignment_policy | varchar(32) | 日历对齐策略 |
| contribution_method | varchar(64)? | C3 方法编码 |
| contribution_baseline | varchar(32)? | A 定义的比较基准 |
| analysis_priority | integer | 合法方向排序 |
| minimum_contribution_threshold | numeric(20,10) | 最小绝对影响占比 |
| absolute_tolerance | numeric(38,10) | 指标单位绝对容差 |
| relative_tolerance | numeric(20,10) | 相对容差 |
| maximum_member_count | integer | 保留成员上限，含 OTHER |
| drill_hierarchy_id | bigint FK? | 下钻层级 |
| requires_review | boolean | 人工确认要求 |

unique(oid, metric_id, logical_dimension_id)。can_contribute 要求可分组、安全关系和完整时间对齐；UNSAFE 不允许发布为可贡献。运行时限制只能比发布能力更严格。

## 15. 语义视图、审核和发布

### 15.1 headless_semantic_view

使用标准资产字段，并增加：

- `default_calendar_id bigint null`；
- `default_model_id bigint null`；
- `visibility_scope jsonb`；
- `query_capabilities jsonb`；
- `active_contract_version_id bigint null`。

约束：`unique(oid, dataset_id, name)`；一个视图只有一个 ACTIVE 发布版本。

### 15.2 headless_semantic_view_item

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `semantic_view_id` | bigint FK | 语义视图 |
| `asset_type` | varchar(32) | `METRIC / DIMENSION / DETAIL_FIELD / ENTITY / MODEL` |
| `asset_id` | bigint | 编辑态资产 ID |
| `display_order` | integer | 排序 |
| `exposure_role` | varchar(32) | `EXPOSED / HIDDEN_DEPENDENCY` |
| `query_usages` | jsonb | 允许用途 |
| `alias_override` | varchar(128) nullable | 视图内别名 |
| `description_override` | text nullable | 视图内说明 |
| `version` | bigint | 版本 |
| `created_at` | timestamptz | 创建时间 |
| `updated_at` | timestamptz | 更新时间 |

约束：`unique(semantic_view_id, asset_type, asset_id)`。

### 15.3 headless_change_request

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 主键 |
| `change_request_id` | varchar(64) | 否 | 外部 ID |
| `oid` | bigint | 否 | 工作空间 |
| `dataset_id` | bigint | 否 | 数据集 |
| `semantic_view_id` | bigint FK | 否 | 发布目标视图 |
| `status` | varchar(32) | 否 | `DRAFT / SUBMITTED / APPROVED / REJECTED / PUBLISHED / CANCELLED` |
| `title` | varchar(200) | 否 | 变更标题 |
| `description` | text | 是 | 变更说明 |
| `base_contract_version_id` | bigint | 是 | 来源发布版本 |
| `asset_version_snapshot` | jsonb | 否 | 提交包含资产和版本 |
| `content_fingerprint` | varchar(80) | 否 | 提交内容指纹 |
| `validation_report` | jsonb | 否 | 提交校验 |
| `submitted_by` | bigint | 是 | 提交者 |
| `submitted_at` | timestamptz | 是 | 提交时间 |
| `created_by` | bigint | 否 | 创建者 |
| `created_at` | timestamptz | 否 | 创建时间 |
| `updated_at` | timestamptz | 否 | 更新时间 |
| `lock_version` | bigint | 否 | 乐观锁 |

约束：

- `unique(change_request_id)`；
- SUBMITTED 后提交快照不可修改；
- 继续编辑必须退回 DRAFT 并形成新的内容指纹。

### 15.4 headless_change_review

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 主键 |
| `change_request_id` | bigint FK | 否 | 变更请求 |
| `review_round` | integer | 否 | 审核轮次 |
| `decision` | varchar(16) | 否 | `APPROVE / REJECT` |
| `reviewed_fingerprint` | varchar(80) | 否 | 审核内容指纹 |
| `comment` | text | 是 | 审核意见 |
| `reviewed_by` | bigint | 否 | 审核者 |
| `reviewed_at` | timestamptz | 否 | 审核时间 |

约束：

- `unique(change_request_id, review_round)`；
- 批准指纹必须等于变更请求当前提交指纹；
- 同一用户允许同时是提交者和审核者，但动作分开记录。

### 15.5 headless_semantic_contract_version

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 发布版本主键 |
| `oid` | bigint | 否 | 工作空间 |
| `dataset_id` | bigint | 否 | 数据集 |
| `semantic_view_id` | bigint FK | 否 | 语义视图 |
| `contract_version` | bigint | 否 | 只增不减版本号 |
| `schema_version` | bigint | 否 | 合约结构版本 |
| `status` | varchar(16) | 否 | `ACTIVE / SUPERSEDED / ARCHIVED` |
| `schema_fingerprint` | varchar(80) | 否 | 发布内容指纹 |
| `compiled_artifact` | jsonb | 否 | 内联的完整 `Published Semantic Contract`；超过内联限制时保存合约头、必要索引、Artifact 引用和内容指纹 |
| `compiled_artifact_id` | varchar(64) | 是 | 完整发布合约超过 JSONB 内联限制时使用的内部 JSON Artifact |
| `validation_report` | jsonb | 否 | 发布校验结果 |
| `approval_snapshot` | jsonb | 否 | 提交审核发布摘要 |
| `change_request_id` | bigint FK | 否 | 来源变更请求 |
| `previous_version_id` | bigint FK | 是 | 上一发布版本 |
| `submitted_by` | bigint | 否 | 提交人 |
| `submitted_at` | timestamptz | 否 | 提交时间 |
| `approved_by` | bigint | 否 | 审核人 |
| `approved_at` | timestamptz | 否 | 审核时间 |
| `approval_comment` | text | 是 | 审核意见 |
| `published_by` | bigint | 否 | 发布人 |
| `published_at` | timestamptz | 否 | 发布时间 |

约束和索引：

- `unique(oid, semantic_view_id, contract_version)`；
- `unique(oid, semantic_view_id) where status = 'ACTIVE'`；
- `idx_contract_dataset(oid, dataset_id, status, published_at desc)`；
- `previous_version_id` 必须属于同一视图且版本号更小；
- 发布后全字段不可修改，只有旧 ACTIVE 状态可改为 SUPERSEDED；
- `compiled_artifact` 与可选 `compiled_artifact_id` 共同表示该 `SemanticView` 在当前版本下的完整、不可变运行态语义合约，不能只保存资产版本列表或分析合约投影；
- `compiled_artifact.catalog` 必须完整描述已发布实体、指标、维度、明细字段、值集合、层级及其可操作能力；
- `compiled_artifact.execution_contract` 必须保存确定性查询编译所需的模型、关系和绑定，但该模块不得直接进入 Agent 上下文；
- `compiled_artifact.generated_indexes` 和 `compiled_artifact.analysis_contracts` 必须可由同版本资产快照重建，并与 `schema_fingerprint` 一致；
- `semantic_context_retrieve` 生成的 Agent 上下文是按问题、权限、Intent 和上下文预算裁剪的运行时投影，不作为新的语义事实版本写回该表。

### 15.6 headless_semantic_contract_asset_version

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 主键 |
| `oid` | bigint | 否 | 工作空间 |
| `semantic_view_id` | bigint | 否 | 语义视图 |
| `contract_version_id` | bigint FK | 否 | 发布版本 |
| `asset_type` | varchar(32) | 否 | 资产类型 |
| `asset_id` | bigint | 否 | 编辑态资产 ID |
| `asset_key` | varchar(160) | 否 | 稳定业务编码 |
| `asset_version` | bigint | 否 | 发布时资产版本 |
| `asset_role` | varchar(32) | 否 | `EXPOSED / DEPENDENCY / ANALYSIS_DEPENDENCY / RELATION / FILTER / TIME_POLICY` |
| `asset_snapshot` | jsonb | 否 | 发布态强类型快照 |
| `content_fingerprint` | varchar(80) | 否 | 资产指纹 |
| `created_at` | timestamptz | 否 | 创建时间 |

约束和索引：

- `unique(contract_version_id, asset_type, asset_id)`；
- `idx_contract_asset_type(contract_version_id, asset_type)`；
- `idx_asset_version_usage(oid, asset_type, asset_id, asset_version)`；
- 只允许发布编译器写入。

## 16. 语义检索表

### 16.1 headless_asset_alias

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `dataset_id` | bigint | 语义项目 |
| `asset_type` | varchar(32) | 资产类型 |
| `asset_id` | bigint | 资产 ID |
| `alias` | text | 中文别名或常见问法 |
| `alias_type` | varchar(32) | `MANUAL / HISTORICAL / BUSINESS_TERM / ABBREVIATION` |
| `language` | varchar(16) | 固定 `zh-CN` |
| `priority` | integer | 检索优先级 |
| `contract_status` | varchar(16) | 编辑发布状态 |
| `version` | bigint | 版本 |
| `status` | varchar(16) | ACTIVE / DISABLED / DEPRECATED |
| `created_by / updated_by` | bigint | 操作者 |
| `created_at / updated_at` | timestamptz | 时间 |

约束和索引：

- `unique(oid, dataset_id, asset_type, asset_id, alias)`；
- `idx_asset_alias_lookup(oid, dataset_id, alias, status)`；
- 别名随资产发布版本冻结并生成 SearchDocument。

### 16.2 headless_asset_relation

该表只保存无法由模型关系、指标输入、指标能力和维度层级表达的通用解释关系，不能替代强类型关系表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `dataset_id` | bigint | 语义项目 |
| `source_type / source_id` | varchar(32) / bigint | 来源资产 |
| `relation_type` | varchar(64) | `RELATED / CONFUSABLE / SYNONYM_CONTEXT / RECOMMENDED_WITH` |
| `target_type / target_id` | varchar(32) / bigint | 目标资产 |
| `weight` | numeric(10,6) | 检索权重 |
| `metadata` | jsonb | 有界解释元数据 |
| `contract_status` | varchar(16) | 编辑发布状态 |
| `version` | bigint | 版本 |
| `status` | varchar(16) | ACTIVE / DISABLED / DEPRECATED |
| `created_at / updated_at` | timestamptz | 时间 |

约束：

- `unique(oid, dataset_id, source_type, source_id, relation_type, target_type, target_id)`；
- 来源和目标不能相同；
- 不允许用通用关系覆盖 `headless_model_relation`、`headless_metric_input_binding`、`headless_metric_dimension_capability` 和 `headless_dimension_hierarchy_level`。

### 16.3 headless_dataset_instruction

该表保存语义管理员维护的业务解释约束，不保存开发维护的 Agent 系统提示词。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint identity PK | 主键 |
| `oid` | bigint | 工作空间 |
| `dataset_id` | bigint | 语义项目 |
| `instruction_key` | varchar(128) | 稳定编码 |
| `module` | varchar(64) | `QUESTION_INTERPRETATION / VALUE_NORMALIZATION / BUSINESS_CONSTRAINT` |
| `content` | text | 中文业务约束 |
| `applicable_asset_refs` | jsonb | 适用资产范围 |
| `priority` | integer | 应用优先级 |
| `contract_status` | varchar(16) | 编辑发布状态 |
| `version` | bigint | 版本 |
| `enabled` | boolean | 是否启用 |
| `created_by / updated_by` | bigint | 操作者 |
| `created_at / updated_at` | timestamptz | 时间 |

约束：

- `unique(oid, dataset_id, instruction_key, version)`；
- 发布态指令进入 ContractAssetVersion 和 SearchDocument；
- 指令不能要求模型生成 SQL、绕过权限、引用未发布字段或改变指标公式。

### 16.4 headless_search_document

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 主键 |
| `oid` | bigint | 否 | 工作空间 |
| `dataset_id` | bigint | 否 | 数据集 |
| `semantic_view_id` | bigint | 否 | 语义视图 |
| `contract_version_id` | bigint FK | 否 | 发布版本 |
| `contract_asset_version_id` | bigint FK | 否 | 发布资产明细 |
| `document_key` | varchar(200) | 否 | 版本内文档编码 |
| `asset_type` | varchar(32) | 否 | 资产类型 |
| `asset_id` | bigint | 否 | 资产 ID |
| `asset_key` | varchar(160) | 否 | 稳定编码 |
| `asset_version` | bigint | 否 | 资产版本 |
| `document_type` | varchar(32) | 否 | `NAME / ALIAS / DESCRIPTION / USAGE / VALUE / ANALYSIS_CAPABILITY` |
| `title` | varchar(256) | 否 | 标题 |
| `keyword_text` | text | 否 | 关键词文本 |
| `embedding_text` | text | 否 | 向量文本 |
| `search_tokens` | tsvector | 否 | PostgreSQL 全文检索 |
| `metadata` | jsonb | 否 | 发布态检索元数据 |
| `status` | varchar(16) | 否 | `ACTIVE / DISABLED` |
| `content_fingerprint` | varchar(80) | 否 | 文档指纹 |
| `created_at` | timestamptz | 否 | 创建时间 |

约束和索引：

- `unique(contract_version_id, document_key)`；
- `idx_search_document_scope(oid, dataset_id, semantic_view_id, contract_version_id, asset_type, status)`；
- `gin(search_tokens)`；
- 文档只能来源于同发布版本资产快照。
- SearchDocument 只保存用于召回的文本和定位信息，不保存 Agent 所需的完整指标、维度或关系结构；命中后必须按 `contract_version_id + asset_key` 回到完整 `Published Semantic Contract` 扩展语义子图。

### 16.5 headless_search_embedding

| 字段 | 类型 | 空值 | 说明 |
| --- | --- | --- | --- |
| `id` | bigint identity PK | 否 | 主键 |
| `document_id` | bigint FK | 否 | 检索文档 |
| `contract_version_id` | bigint FK | 否 | 发布版本 |
| `embedding_model_id` | bigint | 否 | 模型配置逻辑引用 |
| `embedding_model_name` | varchar(128) | 否 | 模型名 |
| `embedding_dimension` | integer | 否 | 向量维度 |
| `embedding` | vector | 否 | pgvector 向量 |
| `content_fingerprint` | varchar(80) | 否 | 来源文本指纹 |
| `status` | varchar(16) | 否 | `READY / FAILED / STALE` |
| `error_code` | varchar(128) | 是 | 生成错误 |
| `created_at` | timestamptz | 否 | 创建时间 |
| `updated_at` | timestamptz | 否 | 更新时间 |

约束和索引：

- `unique(document_id, embedding_model_id)`；
- `embedding_dimension` 必须等于列向量维度；
- 向量索引按部署的 PostgreSQL/pgvector 版本选择 HNSW；
- 来源文档指纹变化时旧向量标记 STALE，不静默继续使用。

